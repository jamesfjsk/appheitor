# Prompt para o Cursor — 23/09/2026 (Prova v3 P1: dados, rotação, tela e painel)

Você é o Cursor do Miner Missions. Antes de qualquer linha de código, leia inteiros: `.cursor/rules/lei-excelencia-aaa.mdc` (inclusive "Conteúdo educativo — a lei do professor"); `docs/etapas/ETAPA_2_LANCAMENTO.md` §1 (decisões 1 a 43; as novas de 22/09 são 38 Torre, 39 teto em dólares, 40 Estante do Sábio, 41 missão sem material, 42 Comerciante invertido; a de 23/09 é a **43, "aprender a aprender"**); `docs/APRENDER_A_APRENDER.md` §1 a §4 e §15 (a lente que muda a repescagem do pacote 11; o resto do documento é desenho para depois e **não** é seu agora); `docs/conteudo/FALAS_APRENDER.md` §1 (as falas do pacote 11, copiadas literalmente); `docs/etapas/ETAPA_3_PROVA_V3.md` inteiro (é a spec, já atualizada em 23/09; os pacotes abaixo são o P1 da §7 dela); em `docs/etapas/REVISAO_ETAPA_2_LANCAMENTO.md`, as seções dos pacotes 5, 6 e 6b (o que o líder já reprovou e por quê) e "Entrega do líder: Estante do Sábio" e "Torre" (o que já existe e você não refaz).

**Estado de partida.** A `main` já tem a Prova v3 P0 (pacotes 5, 6, 6b, 6c), o pacote 7 ("O Sábio lê"), a Estante do Sábio (pacote 9, feito pelo líder), a Torre nova (pacote 8, feito pelo líder), a decisão 41 (`materialsPerTask: 0`) e a 42 (`buyMaterials` no Comerciante). **Não refaça nem reverta nada disso.** Os pacotes 8 e 9 do prompt de 22/09 não existem mais para você. Se discordar de algo, escreva no relatório e siga.

**Método.** Um pacote por vez, na ordem abaixo. Cada pacote fecha com: `npx tsc --noEmit -p tsconfig.app.json` 0 erros; `npx eslint src --max-warnings 8` 0 erros (os 8 avisos são antigos); `npm run test:english` e `npm run test:village` verdes (a pasta `quiz` roda dentro do `test:english`); os testes que cada item pede; fotos 1280×720 e 1920×1080 **só na conta de teste** (`teste@flash.com` / `teste123`, uid `DydxTQ0cGEbX46LLlQxxD123pQD3`); relatório no fim de `docs/etapas/RELATORIO_ETAPA_3.md` com a barra da lei e a lista de arquivos tocados. **Pare para o commit do pai depois de cada pacote.** Nunca a conta do Heitor (uid `xZkTTR2tlIYXIpAelxEqXugNjqo2`). Sem restilizar o painel. Não toque em `cart.ts`, `CartBench.tsx`, na caminhada de `npcBehavior.ts`, em `.mn-village .mn-stage::before`, `bgm.ts`/`vila.mp3`, `src/game/**`, `VillageScene.sceneXY`. Chaves só em `.env` ignorado pelo git. **Geração de prova com IA só quando o item pedir, e no máximo o que ele pede**: o teto mensal de chamadas ainda é o da função publicada e o Heitor usa a mesma conta.

## Pacote 10 — Prova v3 P1, parte 1: dados e rotação (P1.1, P1.2, P1.5, P1.8 e o tempo por pergunta da §6.6)

1. **`src/services/quiz/hash.ts` e `dedupe.ts` (novos, puros)** — P1.1. `normalizeQuestion(q)` → hash (minúsculas, sem acento, sem pontuação, espaços únicos). `nearDuplicate(a, b)` → `true` quando o mesmo `subject` e 70% ou mais das palavras de 4+ letras em comum. Teste com fixture: os 8 pares de `docs/etapas/PROMPT_CURSOR_2026-09-22.md` §"valida.txt" (ou, se o arquivo não existir, 8 pares que você tira dos `dailyQuizzes` reais da conta de teste; nomeie por data) reprovam; 4 pares de assuntos diferentes passam. Ligar em `sanitizeQuestions` (`src/services/aiDailyQuiz.ts`): pergunta com `hash` igual a qualquer uma dos últimos 180 dias do `quizBank`, ou quase igual a uma dos últimos 60, é descartada com o código `repetida` em `sanitize.rejected` e conta para pedir a reserva (`spare`) como as outras.
2. **`quizBank` de verdade** — P1.2. Em `dailyQuizService.completeDailyQuiz`, os 8 docs `quizBank/{uid}_{date}_{n}` entram no **mesmo `writeBatch`** do resultado, com todos os campos da §5.1: `themeId, category, subject, kind, skill, bloom, difficulty, question, options, answer, why, trap, hash, chosen, correct, attempts, secondChoice, retryOk, supportLevel, nudge, msToAnswer, msReadingExplain, audioPlayed, createdAt`. Enquanto a segunda tentativa não existe (pacote 11), `attempts: 1`, `secondChoice`, `retryOk` e `nudge` ausentes, e `supportLevel` é `0` no acerto e `3` no erro (ausente no dilema). As regras publicadas já exigem `userId, date, question, hash, correct, category` na criação e não deixam editar depois: por isso tudo vai no doc na criação. Teste: concluir uma prova (dublê do Firestore como nos testes de `closeQuiz`) grava 8 docs com todos os campos; `msToAnswer` e `msReadingExplain` nunca zerados quando a tela mandou tempo.
3. **Tempo por pergunta** — §6.6. Em `DailyQuiz.tsx`, um `useRef` com `performance.now()` de quando a pergunta entra e de quando ele escolhe, e outro de quando ele apertou "Próxima"; entregues a `completeDailyQuiz` como `timings[n] = { msToAnswer, msReadingExplain }`. Nada aparece na tela.
4. **`pickTheme`** — P1.5 e `ETAPA_2_LANCAMENTO.md` §8.2. Implementar `src/services/quiz/rotation.ts` até `src/services/quiz/__tests__/rotation.test.ts` (escrito pelo líder em 17/09, nunca passou) ficar verde, com as 7 regras da §8.2 na ordem em que estão, e as strings de `reason` exatamente como a §8.2 manda. Ligar em `dailyQuizService.buildAndSave`: histórico dos últimos 90 dias (`getRecentDailyQuizzes(userId, today, 90)` → `ThemeHistoryEntry`), perfil (`learning/{uid}.profile.weak/strong`, se existir; senão vazio), e gravar `theme.angle` e `theme.depth` no doc da prova. Confirmar que `buildPrompt` (`dailyPrompt.ts`) recebe `angle` e `depth` e os escreve no prompt (o P0.4 já previu a assinatura); se não escreve, corrigir. Sem gerar prova nova com IA para provar isso: o teste de `dailyPrompt.test.ts` mostra o ângulo e a profundidade no texto do prompt.
5. **Lista "não repita"** — §8.3 item 1. Confirmar que o prompt manda os **60 enunciados mais recentes** (ordenados da mais recente para a mais antiga, `slice(0, 60)`), não os mais antigos. Teste com 100 hashes datados: os 60 do prompt são os 60 mais novos.
6. **`await prepare()` antes do prefetch de amanhã** — P1.8. Em `DailyQuiz.tsx`, o `ensureDailyQuiz(amanhã)` só roda depois de `await prepare()` da prova de hoje. Teste de integração com `dailyQuizService` mockado: o `avoid` da segunda chamada contém os enunciados da primeira.
7. **`scripts/backfill-quizbank.cjs`** — P2.4, antecipado porque o perfil (pacote 11) precisa de histórico. Lê os `dailyQuizzes` com `questions[]` + `answers[]` do uid dado (`--uid`, `--apply`; sem `--apply` é só leitura) e cria os docs do `quizBank` que faltam, com `hash` e `correct`; idempotente pelo id; usa `scripts/lib/firestore-rest.cjs` como os outros scripts. **Rode só na conta de teste**; o pai roda na do Heitor. Cole no relatório o log da conta de teste (quantos criados, quantos já existiam).

Aceite do pacote 10: testes novos verdes; `rotation.test.ts` verde com os 365 dias; na conta de teste, concluir a prova de hoje grava 8 docs no `quizBank` com tempos (print do console do Firestore ou do script de leitura); o doc da prova traz `theme.angle` e `theme.depth`. Pare para o commit.

## Pacote 11 — Prova v3 P1, parte 2: perfil, tela e painel (P1.3, P1.4, P1.6)

1. **`src/services/quiz/profile.ts` (novo, puro)** — P1.3. `buildProfile(items: QuizBankItem[], today)` com a forma da §5.3 (`byCategory`, `bySubject`, `bySkill`, `byKind`, `strong`, `weak` com mínimo de 4 perguntas, `lastWrong` 10, `englishLastSkill`, `retry` — `{ d30: [ok, n], all: [ok, n] }`, só itens com escada, no lugar do antigo `retryRate` —, `medianMsToAnswer`). `LearningDoc.profile` no tipo (`src/types/village.ts`). Calculado ao fechar a prova (`closeQuiz`/`completeDailyQuiz`, no mesmo batch ou logo depois) e em `computeWeeklyLearning` (`learningService.ts`). Teste com os itens reais da conta de teste exportados para uma fixture: `weak` e `strong` batem com a tabela de acerto por assunto que você calcular à mão no relatório.
2. **Tela da prova** — P1.4, §6 itens 1 a 5, nesta ordem, sem classe nova e sem restilizar:
   - portão da "Próxima": `EXPLAIN_READ_MS.min` de 4000 para **6000** (`provaRules.ts`);
   - explicação em dois blocos: `why` e, abaixo, `trap`, com o rótulo do papiro que já existe; pergunta sem `trap` mostra só o `why`;
   - **segunda tentativa com aviso** (decisões 23b e 43; **substitui a "repescagem com a certa revelada"**, que foi descartada em 23/09; spec em `ETAPA_3_PROVA_V3.md` §6.3 e `APRENDER_A_APRENDER.md` §4.1 a §4.4):
     - `retryable(question)` puro em `provaRules.ts`, pela lista fechada de `skill`: com escada `MAT.*`, `ING.*`, `LIC.APLICAR`, `CIE.CAUSA`, `FUT.TATICA`; a revisita segue o `skill` da original; `LIC.DILEMA` e `skill` ausente ou desconhecido, sem escada;
     - **item com escada, primeira escolha errada**: a certa **não** acende; a escolhida fica marcada e desabilitada; no lugar da explicação vem o aviso de `nudgeFor(question, chosen, seed)` (puro: o `trap` quando ele nomeia a opção escolhida — `trapNamesOption(trap, chosen)`, a mesma regra de `trapHitsDistractor` (`validateQuestion.ts:204`) exportada para uma opção só e usada nos dois lugares — e `answerLeaksInPrompt(trap, answer)` é falso; senão uma pista de `FALAS_APRENDER.md` §1.1 pela área do `skill`, sem repetir a mesma em 14 dias); o aviso é falado com a voz da prova; em inglês, o `audioText` toca de novo; as opções reabrem depois de `readingMs(aviso, 3000, 8000)` e do fim do áudio;
     - **segunda escolha**: certa, uma linha `desc_*` + `why` e `trap`; errada, a certa acende + uma linha `seg_*` + `why` e `trap`; depois, o portão da "Próxima" como sempre;
     - **item sem escada, errado**: a certa acende + uma linha `fato_*` + `why` e `trap`; **sem** segunda tentativa;
     - **sai "Não foi dessa vez."** (`DailyQuiz.tsx:661`); o acerto de primeira continua "Isso."; as linhas vêm de `src/data/helpLines.ts`, criado por você copiando `FALAS_APRENDER.md` §1 literalmente;
     - `answers[]`, `score`, `reward` e a esmeralda só olham a **primeira** escolha; o `quizBank` grava `attempts`, `secondChoice`, `retryOk`, `supportLevel` (0 de primeira; 1 acertou depois do aviso; 3 errou as duas ou errou item sem escada) e `nudge` (`'trap' | 'strategy'`); a regra do Firestore aceita os campos (`hasAll`), nada a publicar;
     - **sem frase dizendo que a segunda não paga** (23b);
     - teste: `retryable` com um `skill` de cada coluna; `nudgeFor` nos três casos (o `trap` nomeia a escolhida e não vaza → `trap`; o `trap` vaza a resposta, como "quem responde 30 parou no meio; o total é 120" → pista da área; a escolhida não é a do `trap` → pista da área); `supportLevel` nos caminhos 0, 1 e 3;
   - inglês, áudio primeiro: quando `question.subject === 'ingles'`, toca `audioText` com `{ lang: 'en', speed: TTS_SPEED_SLOW }` e só libera os cliques quando terminar (reusar `voiceDone`); botão de repetir o áudio ao lado do enunciado (`mc-slot`, 44 px); `audioPlayed: true` no `quizBank`;
   - reflexão: molde tocável ("Hoje eu … porque …") que preenche o começo do campo, **só enquanto `village.stats.reflections < 10`** (depois some; decisão 43); contador "7 / 12" ao lado de "Entregar", sempre; `reflectionOk` passa a exigir **12 palavras (8 nos 7 primeiros dias desde `village.launchedOn`) e 2 palavras de conteúdo do tema**; grava `reflectionWords`, `reflectionThemeHits` e `reflectionMs` no doc da prova (§5.2). Teste em `provaV2.test.ts`: `readingMs(texto, 6000, 12000) === 6000` para 9 palavras; reflexão de 11 palavras reprova depois do 7º dia e aprova antes.
   - Fotos: uma conta errada com o aviso e a certa apagada; a segunda tentativa certa com a linha `desc_*`; um item de fato errado, sem segunda tentativa; a explicação com os dois blocos; o contador "7 / 12"; o molde. Conta de teste.
3. **Painel "Como ele vai"** — P1.6, §6 último bloco, no fim de `DailyQuizManager.tsx`, com os cartões de pedra que já existem no arquivo: acerto por `kind` e por `subject` em 7, 30 dias e total (do `learning/{uid}.profile`); as últimas 30 erradas com data, assunto e o que ele marcou (do `quizBank`); repetidas (mesmo `hash`, as duas datas); do doc do dia, "gerou N, descartou M" com os códigos de `sanitize.rejected` e os motivos do revisor; uma linha da reflexão (palavras e se tocou o tema); uma linha "Depois do aviso: acertou X de Y nos últimos 30 dias" (`profile.retry.d30`), com "pouco dado ainda" abaixo de 8. Foto do painel na conta de teste depois de rodar o `backfill` dela. O pai tem que ler em 10 segundos em que ele vai mal.

Aceite do pacote 11: testes verdes; fotos; `learning/{uid}.profile` da conta de teste existe com `strong`, `weak` e `lastWrong` (print). Pare para o commit.

## Pacote 12 — pequeno: ruído no `clientErrors`

Desde 20/09 o `clientErrors` recebe rajadas de "useAuth deve ser usado dentro de AuthProvider" e "useData deve ser usado dentro de DataProvider" com `userId` vazio. A pilha aponta `ProtectedRoute` → `useAuth` em `http://localhost:5173/...?t=…`: é o hot reload do Vite trocando o módulo do contexto, não o app publicado. Em `src/services/observability.ts`: (1) não gravar quando `location.hostname` é `localhost` ou `127.0.0.1` (só `console.warn`); (2) gravar `href` e `userAgent` curtos em todo erro, para a próxima leitura dizer de onde veio; (3) teste puro da função de decisão (`shouldReportClientError({ hostname, message })`). Sem mexer no `ProtectedRoute`. Pare para o commit.

## O que fica com o líder (não é seu)

Revisão dos pacotes 10 a 12; os prompts dos pacotes AP1 a AP5 da decisão 43 (`APRENDER_A_APRENDER.md` §12; nada deles entra antes); leitura da prova de 24/09 do Heitor (primeira v3 em produção); `docs/MANUAL_DO_PAI.md`; desenho dos comprovantes das missões de vida real (decisão 24, depois da Estante); arte do Túnel e do mirante; Expedição do Explorador.
