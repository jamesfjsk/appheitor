# Prompt para o Cursor — 22/09/2026 (correções da revisão + Prova v3)

Você é o Cursor do Miner Missions. Antes de qualquer linha de código, leia inteiros: `.cursor/rules/lei-excelencia-aaa.mdc` (inclusive "Conteúdo educativo — a lei do professor"); `docs/etapas/ETAPA_2_LANCAMENTO.md` §1 (decisões 1 a 36 — a 23 manda: recompensa só na primeira tentativa, retentar é treino, **sem aviso explícito na tela**), §9 e §12; `docs/etapas/REVISAO_ETAPA_2_LANCAMENTO.md` nas seções "22/09, manhã", "Revisão dos pacotes do fim de semana (22/09)" e "Correções do líder (22/09, tarde)" — cada achado tem id (A2, F1, R3, T1…), arquivo:linha e a correção esperada; `docs/etapas/ETAPA_3_PROVA_V3.md` inteiro.

**Estado de partida.** A `main` já contém a Prova v2, Comerciante V1, Recado V1, Cofre, hotfix de ruína e Torre na cena, mais as correções do líder (A13, preparo por dia, F01, A2, A3, F1, R1, L1). Não refaça nem reverta nada disso; se discordar, escreva no relatório. Congelados (não tocar): `src/services/village/cart.ts`, `CartBench.tsx`, a caminhada em `npcBehavior.ts` (constantes e `heroClickPlan`), `.mn-village .mn-stage::before`, `src/services/bgm.ts`, `src/game/**`, `VillageScene.sceneXY` e o desenho com `object-fit: contain`. Não restilize o painel do pai. Nunca use a conta do Heitor; teste com `teste@flash.com` / `teste123` (admin `admin@flash.com` / `admin123`). Nenhum segredo fora do `.env`.

**Método.** Um pacote por vez, na ordem abaixo. Cada pacote fecha com: `npx tsc --noEmit -p tsconfig.app.json` 0 erros; `npx eslint src --max-warnings 8` 0 erros; `npm run test:english` e `npm run test:village` verdes; os testes que cada achado pede; fotos 1280×720 e 1920×1080 de toda tela tocada, tiradas do build atual (Playwright, conta de teste), em `docs/exemplos/telas/etapa-3/<pacote>/`; linha no §12 do ETAPA para todo arquivo novo. Relate em `docs/etapas/RELATORIO_ETAPA_3.md`, uma seção por pacote, no formato de sempre (O que mudou / Arquivos / Barra / Como verificou / Fora / Dúvidas), citando o id de cada achado e o que ficou de fora. **Pare para o commit do pai depois de cada pacote.** Dúvida se resolve no relatório, não inventando.

## Pacote 1 — Prova do dia (o que ainda sangra)

M1 guarda `completed === true` no início de `completeDailyQuiz` (o retry ainda chega ao `payQuizRewards`); M2 gravar `answers`, `score`, `totalQuestions` e `awaitingReflection: true` ao fechar a 8ª pergunta (sem `completed`, sem pagar) e dar saída na tela da reflexão sem perder as respostas; M3 anel nunca cheio enquanto travado (degraus de 25% com reduced motion); M4 tirar `disabled={!voiceDone}` do botão da lição (o clique já corta a voz); M9 `dilemmaOf` devolve `null` quando o índice 2 não é o dilema; B4 passar o `about` em `completeDailyQuiz` e gravar `reflectionWords`; B5 apagar `saveReflection`; B1 registrar `{ name: 'quiz', dir: src/services/quiz/__tests__ }` no harness — `rotation.test.ts` vai ficar vermelho porque `pickTheme` é um stub: relate, não implemente aqui (é P1.5 da v3). Testes: dublê de `payQuizRewards` que lança (o doc não fica `completed` sem claim); `dilemmaOf` com lição descartada; `reflectionOk` com `about`.

## Pacote 2 — Comerciante V1

F2 um pedido = uma colocação, travado na origem, com teste; F3 rótulo da preposição só em `padMode 'hint'` e pedido 1 sem a zona acesa; F4 `missKind(step, placed)` por tentativa em `details` (`relation | item | qty | spot`); F5 falas em português com `{ lang: 'pt', speed: TTS_SPEED_TALK }` (frases mistas em duas chamadas); F6 `correctionFix` devolve `{ en, pt }`, o `en` fica na tela e toca; F8 `onClick` no item e no lugar com `playText(palavra, { lang: 'en' })` — sem `playText` dentro de `markHover`; F9 `baseY` na âncora de parede em `anchors.json`; F11 `boots` fora de qty ≥ 2 (ou "two pairs of boots" com validador); F12 cama e cerca fora do armazém, `under` fora de porta/cerca, `in the oven` só com itens de cozinha; F13 itens e textos da bandeja em unidade do palco; F14 `listens` só quando o áudio tocou, `textShown` com o sentido real; F15 reescrever `docs/MINA_CONTRATOS.md` §3.1 com o jogo entregue. Consertar `_shot_comerciante.mjs` (hoje solta o item em frações fixas e sempre erra) e refotografar: as fotos 01-07 atuais são de outro build e não valem como aceite. F16 = decisão 34: depois de dois erros a criança continua tentando até acertar, sem pagar e sem aviso (23b); o Comerciante só vira a página quando o pedido fecha; registrar todas as tentativas em `details`.

## Pacote 3 — Recado do Capataz V1

R2 falta de informação na 1ª tentativa não pode zerar o Recado nem virar "primeira leitura" grátis: usar teto de material para "teve ajuda", como a dica já faz; R3 guardar `firstAnswer` junto de `firstJudge` e mostrar as duas; R4 `moldFromModel` só com 2+ lacunas e sem terminador de frase dentro do slot; R5 `teachFromRecado` deixa de repetir o brief inteiro; R7 corrigir o banco (n2-05 causa invertida — trocar o motivo por um desejo, mantendo `because`; n2-03 tirar "because I am new"; n2-01 brief) com teste; R8 falas em português com `PT_TALK` compartilhado com `provaSpeak.ts`; R10 `dirty` só com o que a criança digitou e listener de Esc com deps certas; R11 não apagar o que ela montou na pré-checagem, com trava para o 2º "Enviar" não virar a tentativa que paga; R12 ligar `chalkDiff` e `pegLesson` na fase `judged` (o código e o CSS já existem); R14 usar ou tirar `brief` do Finale; R15 inverter a guarda de vazamento (`say.includes(model)`); R16 bandeja a partir de 22%; R18 fontes em unidade do palco. R6 = decisão 34: no degrau 1 a bandeja traz só o `wordBank` (10-14 palavras, forma base, minúsculas); o que faltar ele digita; a frase inteira continua no Ouvir/Ouvir devagar; ajustar o teste `notePlay.test.ts:135` que hoje exige a frase inteira na bandeja. R17 (dica grátis na Biblioteca n3) aguarda o pai.

## Pacote 4 — Cofre, Torre e regras

T1/T2 apagar `coverPaintedLookout` e a chamada (o líder repinta o mirante no fundo depois); C1 sempre `createGoal` no caminho do prêmio e a barra do prêmio soma os montinhos do mesmo `rewardId`; C2 reaproveitar montinho fantasma (`open`, `savedGold 0`) antes do teto; C3 GoalsPanel filtra `cancelled` resgatados; R2 (Cofre) abortar a transação sem doc `progress`; D14 `cofre.liveMaxLevel: 2` + `opensIn: 'Etapa 3'`, Biblioteca liberada até 3 com os textos da decisão 14 e o estado "no teto da etapa, abre depois" no `BuildingCard`; L2 extrair `previewLevelFrom(id, level, hostname, search)` pura com teste real; T3 resolver o preview na carga do módulo e ancorar o host; F05 regra de `goals`: `unlockOn` só cresce quando já existe no doc; F06 `village` proíbe também `launchedAt`, `userId`, `createdAt`; publicar as regras e registrar a hora; F07 tirar `ISO_NPC_WALK` dos arquivos que não existem sem tocar no `useWalk` do herói; F08 `git rm -r --cached functions/lib`. R1 = decisão 36: `processPendingDays` derruba no máximo 2 obras por rodada de dias atrasados; os demais só tiram tocha; férias continuam sem ruína; teste com 7 dias em fila.

## Pacote 5 — Prova v3, P0

Decisões tomadas pelo pai em 22/09 e escritas no §1 do ETAPA: 32 (v3 aprovada como está), 33 (dilema fora da nota: `kind: 'dilemma'`, M8), 34 (R6 e F16 = A), 35 (jogo morto fica como está — não toque em `EnglishArena`/`MineRush`/`RedstoneBench`/`phaser`), 36 (teto de 2 ruínas por rodada). Depois do pacote 4: P0.2 a P0.9 da §7 na ordem (P0.1 já está feito), cada um com o teste e a evidência da tabela, mais 3 provas geradas de verdade na conta de teste com o JSON de `sanitize` e as 8 perguntas coladas no relatório, para o líder aplicar as seis perguntas da lei do professor.

## Pacote 6 — Prova v3: calibrar o validador e fechar o P0 de verdade (opção B, decidida pelo pai em 22/09)

Contexto: as três provas geradas no pacote 5 foram para o banco offline e a sonda do líder (`docs/exemplos/telas/etapa-3/prova-v3/sonda-lider-2026-09-23-raw.json`) mostra a IA devolvendo 8 perguntas razoáveis e o validador reprovando as 8. Leia `REVISAO_ETAPA_2_LANCAMENTO.md`, seção "Etapa 3, pacotes 4 e 5", e `ETAPA_3_PROVA_V3.md` §4 (validador), §4.3 (20 casos), §4.4 (revisor) e §7 (P0.5, P0.6). Se o modo observação (`QUIZ_VALIDATOR_ENFORCE`) já entrou, mantenha a constante e ligue-a (`true`) só quando o aceite abaixo passar.

1. **`why_sem_resposta`**: aprova se o `why` contém pelo menos uma palavra de conteúdo (4+ letras) da resposta, ou o número da resposta, ou as 3 primeiras palavras dela — nunca a string inteira. Teste com a Q1 da sonda ("Influência dos imigrantes portugueses" / "A presença de imigrantes portugueses…") aprovando.
2. **`sinonimas`**: apagar o código, ou ignorar pares que diferem em uma única palavra (par mínimo: "Mais/Menos oxigênio", "a/an apple"). Teste com os dois pares da sonda aprovando; `optionsCollide` continua pegando iguais.
3. **`audio_mismatch`**: comparar `audioText` com a resposta quando o enunciado não tem frase em inglês ("Which sentence is correct?"). Teste com a Q8 da sonda aprovando.
4. **Códigos que faltam da §4**: `opiniao` (regex da spec: "o que você faria / como você agiria / o que você acha / você prefere / o que seria mais sábio / como você poderia usar / se você tivesse que escolher", fora do dilema), `opcao_caricata` (lista da spec: ignorar, fingir, burro, falar mal, não fazer nada, impor, não se importar, sair do jogo, criticar), `tamanho_opcoes` (maior ≤ 1,5× a menor em palavras ou diferença ≤ 2), `certa_mais_longa`, `tipos_mistos`. A Q2 da sonda ("Como você pode aplicar o conhecimento sobre sotaques na escola?") tem que cair em `opiniao`. O `CARICATURA` atual (célula azul) pode ficar com outro nome.
5. **Os 20 casos reais da §4.3** viram testes sobre `fixtures/era3.json` + a sonda do líder, com o veredito esperado de cada um (21/09 Q4 aprova; 15/09 Q8 aprova no local).
6. **P0.5 de verdade**: pedir `count + spare` (a IA devolveu 8 de 11 — reforçar no JSON: `"questions": [ exatamente 11 objetos ]`); se sobrarem menos de 8, **uma chamada de substituição** pedindo só as que faltam, com os motivos dos descartes e os enunciados aprovados como `avoid`; se ainda faltar, **completar** do banco offline passando pelo validador — nunca trocar a prova inteira. Guardar `sanitize.perQuestion` (código por pergunta) e a saída crua da IA (`raw`) no doc para calibração.
7. **P0.6 de verdade**: revisor `gpt-4o-mini`, temperatura 0, `response_format json`, prompt da §4.4, devolvendo `{itens:[{n, ok, motivo}]}` sobre as aprovadas no local; reprovada sai e entra substituta; resposta malformada não derruba a prova. O `reviewBatch` local pode ficar como log, mas o revisor é a chamada.

8. **Explicação em português, sempre** (pedido do pai em 22/09, ao ver a Q4 de inglês da conta de teste com explicação toda em inglês). No prompt (`dailyPrompt.ts`), regra explícita para `why`, `trap` e `explanation`: "em português do Brasil; inglês só nas palavras ou frase entre aspas; nomeie a regra em português com um exemplo" — ex.: "Depois de 'yesterday' o verbo vai para o passado: 'defended'. 'Defends' é o presente, de todo dia." No validador, código `explicacao_em_ingles`: reprova quando, tirando o que está entre aspas, menos de 40% das palavras do `why` estão numa lista curta de palavras funcionais do português (é, de, que, não, porque, para, com, um, uma, o, a, os, as, do, da, em, quando, depois, antes, verbo, frase, regra). Teste com a Q4 real ("The past tense 'defended' correctly fits the sentence context.") reprovando e com o exemplo acima aprovando.
9. **Inglês sem duas certas por tempo verbal.** No prompt: "quando as opções são formas do mesmo verbo, a frase traz uma marca de tempo que só aceita uma forma (every day, always, now, right now, yesterday, last week, tomorrow, next week)". No validador, código `ingles_sem_marcador`: se `subject` é inglês e 3+ opções são formas do mesmo verbo (mesma raiz de 4 letras), a frase (`question` ou `audioText`) precisa conter uma marca da lista; senão reprova. Teste com "The knight ___ the castle bravely." (defends/defended/defense/defending) reprovando e com "The knight defended the castle yesterday." aprovando. O revisor (P0.6) continua sendo quem pega "duas defensáveis" fora desse padrão.

10. **Decisão 37: futebol é cenário, não matéria** (pedido do pai em 22/09). Prompt (`dailyPrompt.ts`): a posição de futebol passa a ser "cenário de futebol": a pergunta ensina outra área — MAT.OP2 (gols, minutos, pontos na tabela, duas etapas), CIE.CAUSA (curva da bola, gramado molhado, fôlego), leitura de tabela ou gráfico, lógica, inglês no nível (frase da partida), ou decisão de capitão (dilema) — e o `subject` é o da área ensinada, com o campo novo `scenario: "futebol"`. Proibido, com exemplos negativos no prompt: regra ("o que acontece se sair do campo", "quantos jogadores"), história, definição, "quem é o melhor/mais famoso". Tirar `FUT.REGRA` da lista de códigos; a rotação por dia da semana troca "futebol" por "cenário de futebol". Validador: código `futebol_solto` quando `subject === 'futebol'` ou quando o enunciado casa regra/definição de futebol (regex: "o que acontece se", "quantos jogadores", "qual a posição", "quem é o", "regra do", "impedimento", "cartão" sem número na pergunta). Teste com a Q6 real da conta de teste ("Se um jogador está fora de campo durante o jogo, o que deve acontecer?") reprovando e com "2 gols no 1º tempo, sofreu 3, terminou 4 a 3" aprovando. Registrar em `ETAPA_3_PROVA_V3.md` como regra 27.

11. **Revisor: "na dúvida, aprove" deixa rastro** (aprovado pelo pai em 22/09). No prompt do revisor (`reviewer.ts`), cada item devolve também `"duvida": true` quando a aprovação foi no limite; `parseReview` lê o campo (ausente = false); `applyReview` mantém a pergunta e devolve a lista de dúvidas; o doc grava `sanitize.duvidas: [{ n, question, motivo }]`; o painel mostra a contagem junto de `kept`/`dropped`. Teste do parser com e sem o campo.

Aceite (sem isso o pacote não fecha): 3 gerações reais na conta de teste (`?quiz=regen`, 3 datas) com **≥ 8 perguntas da IA mantidas em cada uma e zero prova offline**; no relatório, para cada prova: a saída crua, o código de cada pergunta descartada, o motivo do revisor e as 8 finais; tsc 0, eslint 0 erros, `test:english` e `test:village` verdes; o líder aplica as seis perguntas do professor nas 24 finais. Junto: D14 de volta (`mesa.liveMaxLevel: 1`, `opensIn: 'Etapa 3'`, textos novos ficam; `levels.test.ts`). Pare para o commit.

## Pacote 7 — "O Sábio lê": vida na espera da reflexão (pedido do pai em 22/09)

Em `DailyQuiz.tsx`, o estado `saving` deixa de ser rótulo de botão e vira cena: (1) botão "Entregar" some, textarea fica esmaecida e só leitura com a frase dele; (2) o balão "O Sábio pergunta" passa a mostrar falas em sequência, cada uma ≥ 1,6 s, total ≥ 2,4 s mesmo se a IA responder antes: "Deixa eu ler com calma…" → "Hum. Lendo de novo a sua frase…" → "Pensando no que você quis dizer…" → "Quase lá."; reticências ganham um ponto a cada 400 ms (fixas com prefers-reduced-motion); (3) `.mn-papiro-face` balança ±2° em 1,2 s ease-in-out em loop enquanto lê (parado com reduced-motion); (4) som curto de pena (≈300 ms, WebAudio, via `useSound`) ao começar; carimbo existente no veredito; (5) veredito entra com fade no lugar da última fala, com `say` e carimbo; recusa devolve a caixa editável com a frase mantida; (6) sem "processando/carregando/aguarde/spinner". Regras puras (sequência de falas × tempo, mínimo de 2,4 s) em `provaRules.ts` com teste. Fotos 0 s / 2 s / veredito em 1280 e 1920 na conta de teste. Pare para o commit.

## Pacote 6b — o que ficou do pacote 6 (revisão do líder, 22/09)

Os itens **8, 9, 10 e 11** do pacote 6 não entraram (não existem `explicacao_em_ingles`, `ingles_sem_marcador`, `futebol_solto` nem `duvida`): fazer agora, como escritos acima. Mais dois, vindos da leitura das 24 perguntas do aceite:

12. **Substituição por posição, não por quantidade.** Quando uma pergunta cai, a chamada de substituição pede a mesma posição com o mesmo `kind`/`skill` (LIC.APLICA, LIC.DILEMA, a área de conhecimento daquela posição) — hoje ela pede "as que faltam" e as extras 9-11 (matemática/ciências/inglês) entram no lugar da lição e do dilema, e a prova fica com 1 pergunta do tema, duas contas iguais e duas frases iguais de inglês (16/11). No prompt, para LIC.APLICA e LIC.DILEMA: "as 4 opções têm o mesmo tamanho (± 2 palavras) e nenhuma é caricata" com um exemplo bom — é por isso que as três provas do aceite saíram **sem dilema** (decisão 33) e sem aplicação. Aceite: nas 3 provas novas, posições 1-3 são LIC.IDEIA, LIC.APLICA e LIC.DILEMA (`kind: 'dilemma'`), e nenhum `skill` se repete mais de duas vezes.
13. **Definição escapando pelo "qual é a função de".** `DEFINICAO` passa a cobrir "qual é a função d", "qual é o papel d", "o que faz o/a", "para que serve"; junto com `futebol_solto` (item 10) isso mata o padrão "Qual é a função do goleiro/árbitro/cartão vermelho no futebol?", que apareceu em 19 das ~25 provas geradas na sessão (três na de 29/10). Teste com essas três frases reais reprovando.

Aceite do 6b: 3 gerações reais com ≥ 8 da IA, dilema presente nas 3, zero "função do X no futebol", `why` em português, `sanitize.duvidas` gravado; barra verde; relatório com raw e códigos. Pare para o commit.

## Pacote 8 — Torre: uma conquista só, que se explica (pedido do pai em 22/09; detalhe em `REVISAO_ETAPA_2_LANCAMENTO.md`, seção "Torre")

Decisão 38 do §1 (aprovada pelo pai em 22/09, itens a, b e c). Fazer: (1) `AchievementsBadges` sai da Torre; a aba "Vida real" lista as categorias Rotina, Agenda e Baú do catálogo novo e "Conquistas" as demais; `AchievementManager` sai do `ParentPanel`; script `scripts/archive-legacy-achievements.cjs` marca `archived: true` nos 13 docs de `achievements` (não apaga); (2) cartão novo em `Torre.tsx`: título, descrição, medalha com nome do tier, prêmio escrito, barra `min(atual, alvo)/alvo`, data; ícone por família + número do degrau no canto; "Segredos" sem contagem e só depois do primeiro desbloqueado; no máximo 6 grupos por aba; (3) `REWARD` em `data/achievements.ts`: XP 30/75/150/300; gold 3/6/12 só em rotina/agenda/bau (bronze/prata/ouro), pago em `villageService` dentro de `achievementGoldCap` semanal com linha em `goldTransactions` (`source: 'achievement'`); material do nível da Ferraria em vez de madeira fixa; `rewardHasGold` de verdade; (4) `SEVEN` sem `campinho` em `services/village/achievements.ts` e teste; (5) testes: `Faltam` nunca negativo, progresso limitado, gold só nas três categorias, teto semanal. Fotos 1280/1920 das duas abas com a conta de teste. Pare para o commit.

## Pacote 6c — teto de IA em dólares (decisão 39, 22/09; antes do pacote 7)

O teto de 800 chamadas estourou com as gerações de teste dos pacotes 5-6b e derrubou prova, contratos, juiz e **voz** do Heitor (a checagem em `functions/src/index.ts:148` vem antes de separar `chat` de `tts`). O pai decidiu: o teto passa a ser **custo estimado do mês em dólares**, alto, só para o caso alarmante.

1. `functions/src/index.ts`: apagar `AI_MONTHLY_CALL_CAP`. Criar `functions/src/aiPrices.ts` com a **mesma tabela** de `src/services/aiCost.ts` (US$ por milhão: entrada e saída por modelo — `gpt-4o` 2,50/10,00; `gpt-4o-mini` 0,15/0,60; `gpt-4.1-mini` 0,40/1,60; voz `gpt-4o-mini-tts` 15,00 por milhão de caracteres) e `estimateMonthUsd(usageDoc)`; a função recusa (`resource-exhausted`, mensagem "Teto mensal de IA: US$ 50") **só** quando `estimateMonthUsd >= AI_MONTHLY_USD_CAP = 50`, e a checagem vale para chat e voz igualmente (é custo). Para isso `bumpUsage` precisa gravar tokens **por modelo** (`tokensByModel.<modelo>.in/out`) além do total — hoje só grava o total.
2. `src/services/aiCost.ts`: `AI_MONTHLY_USD_CAP = 50`, `AI_MONTHLY_USD_WARN = 40`; `estimateCostUsd` usa `tokensByModel` quando existir (senão a média atual); teste: 1 000 tokens de entrada em `gpt-4o` custam 6,25× o `gpt-4.1-mini`; teste que compara a tabela do cliente com a da função (regex sobre `functions/src/aiPrices.ts` ou import direto).
3. Painel (`UsageCard`): "Gasto estimado: US$ X de 50" com barra, chamadas de texto e de voz separadas, custo por modelo; cartão Hoje do painel avisa a partir de US$ 40. Nada de restilizar.
4. `isOverCap` no cliente passa a olhar o custo, não as chamadas.
5. `_gen_prova_v3.mjs` só gera as datas do aceite (3) — nada de lote. Depois que o pai publicar a função, gerar as 2 provas que faltam do aceite do 6b e colar no relatório (raw, códigos, dúvidas, 8 finais).

Barra verde; `npm run test:village` com os testes novos. Pare para o commit. O pai publica a função (`npx firebase-tools deploy --only functions --project app-heitor`) e sobe o limite rígido da conta OpenAI para US$ 60.
