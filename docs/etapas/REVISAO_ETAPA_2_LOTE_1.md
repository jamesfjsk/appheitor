# Revisão da Etapa 2, Lote 1

15/09/2026, 15h. Relatório do Cursor: `RELATORIO_ETAPA_2_LOTE_1.md`. Especificação: `ETAPA_2_BANCO_E_TEMPORADA.md` (seções 1 a 8, 13, 16, 17, 18) e os documentos de desenho `VILA_CONSTRUCOES.md`, `VILA_ITENS.md`, `VILA_MAPA.md`. Método: checagens mecânicas, três revisores independentes (dinheiro e regras, aderência com conferência de conexões, experiência da criança) e teste no navegador na conta de teste (seção 8; resultado entra quando terminar).

**Aviso importante**: enquanto a revisão rodava, o Cursor continuou editando o lote ("ajustes visuais"). Entre 14h50 e 15h00 entraram mudanças em `HeroPanel`, `VillageHome`, `VillageScene`, `BuildingCard`, `Cofrinho`, `englishBase.ts`, `village.ts`, `bank.ts`, `goalsService.ts` e `englishBaseService.ts` que **regrediram quatro regras fechadas** (seção 2, itens A4 a A7). Esta revisão é uma foto das 15h07; tsc e testes passavam nesse instante. Não fiz nenhum ajuste no código desta vez, porque o Cursor está com os arquivos abertos; tudo volta para ele.

## 1. Veredito

**Não aprovado ainda.** A espinha está certa e é muita coisa boa: relógio único de Brasília com correção pelo servidor e virada do dia; economia v2 (curva, faixas, `minLevel`, prova linear, custo x2, Comerciante); Cofrinho com depósito, juros, cancelamento e fechamento pelo pai; desafios ligados nos cinco eventos; tetos do gold do jogo; Baú do Dia em escada; capacete, Cerca, Fundição e Queima; conserto e missão recuperada; regras e índices publicados; Casa com as missões; Mercado com prêmios embutidos; Banco com três abas; cabeçalho com chips que abrem coisas; painel com metas, desafios, faixas e Balança; função `openai` e `agendaReminders`; simulador.

O que impede de fechar: cinco furos de dinheiro e de regra confirmados no navegador (os três abaixo, mais a forja quebrada pela curva antiga e a missão da Agenda pagando gold) (missão recuperada pagando duas ou três vezes; missão extra contando como devida no fechamento, no Baú e no conserto; missões "Foco" pagando todo dia para sempre), três regressões das edições em andamento (prova deixou de ser portão; cartão da construção inalcançável depois do nível 1; juros por nível do Cofre contra a decisão de 5%; Mercado e Agenda como construções foi decisão do pai e fica, com ajustes), a Agenda entregue sem nenhuma das ligações prometidas na seção 13, a lanterna forjável sem efeito, e o aceite no navegador não feito pelo Cursor.

## 2. Severidade alta (bloqueiam o Lote 1)

| # | Onde | Problema | Correção |
|---|---|---|---|
| A1 | `firestoreService.ts` (caminho `late`), `DailyChecklist.tsx:191` | Missão recuperada: a lista "Recuperar" mostra toda missão devida ontem cujo `lastCompletedDate !== ontem`, e a transação só recusa se `lastCompletedDate === ontem`, depois grava `lastCompletedDate = ontem`. Missão feita hoje aparece como recuperável, paga metade de novo, volta a pendente e pode ser concluída de novo com gold cheio: até três pagamentos em 24 h. | Na transação: ler `taskCompletions` (taskId, date = ontem, não revertida) e recusar se existir; nunca sobrescrever `lastCompletedDate` quando já é hoje (gravar só a completion de ontem com `late: true`). Na tela, listar como recuperável só o que não tem completion de ontem. |
| A2 | `dailyRulesService.ts` (`completionsOn`), `villageService.ts` (`openDailyChest`, `repairLot`) | Missões extras (inclusive as "Foco" da Agenda) contam como feitas para as devidas: 3 devidas, faz 2 + 1 extra → sem penalidade, tocha somada, Baú abre, conserto passa. A spec diz que a extra "nunca conta como perdida", não que substitui uma devida. | `closeDay`: `missed = missedTasks.length` e `done = due - missed`; `openDailyChest` e `repairLot`: contar só completions cujo `taskId` está em `dueTasksOn(tasks, today)`. |
| A3 | `agendaService.ts` (`acceptStudyPlan`), listener de `tasks` em `firestoreService.ts`, `DailyChecklist.tsx:74`, `firestore.rules` | As três missões "Foco" nascem `daily` + `optional` com um campo `date` que ninguém lê: aparecem todas hoje e todo dia, pagam 10 XP + 2x material para sempre. A regra de `create` da criança para `origin == 'agenda'` não limita `xp` nem exige `date`. | Mapear `date` no tipo e no listener; extras só com `!date || date === hoje`; transação recusa `date !== hoje`; desativar depois da data; regra: `xp <= 10 && gold == 0 && optional == true && date is string`. |
| A4 | `HeroPanel.tsx:42`, `VillageHome.tsx` (edição em andamento) | A prova deixou de ser portão (`quizLocked` sumiu; `gated={false}`) e nem abre enquanto a Mesa não for construída (`VillageQuiz` devolve `null` com `buildings.mesa < 1`). Conta nova com `quizRequired` não tem caminho para a prova e abre Mina, Ferraria, Mercado e Cofre sem ela. Quebra o invariante 7 e `VILA_MAPA` ("cada lugar com o mesmo cadeado da prova"). | Restaurar `quizLocked` e os cadeados em `mine`, `market`, `build:*`, `npc:ferreiro`, `npc:comerciante`; renderizar a prova sempre que houver `onboardedAt`; a Mesa nível 1 libera só o tema de amanhã. |
| A5 | `config/englishBase.ts:203-220`, `VillageHome.tsx`, hotbar | **Reclassificado: decisão do pai (15h45).** Mercado e Agenda como construções ficam (Barraca do Comerciante e Sino da Vila, `VILA_CONSTRUCOES.md` 9 e 10). O que ainda precisa: custo de nível 1 igual ao da Fornalha e sem pré-requisito; hotbar "Mercado" de volta, abrindo a Barraca ou, antes dela, o cartão de construir com os prêmios de verdade visíveis; só nível 1 por enquanto; sprites próprios (eu gero); os campos novos de `englishBase` declarados em `fromBaseDoc` (feito) e nas regras. | Ajustar custo, pré-requisito, hotbar e cartão; sprites pelo líder. |
| A6 | `VillageHome.tsx:162-190` (edição em andamento) | `openDistrict` intercepta `build:fornalha` (nível 1 ou mais → Ferraria), `build:mesa` (→ prova), `build:cofre` (→ Banco), `build:torre` (sempre → Torre) antes de abrir o cartão. Depois do nível 1 não existe caminho para "Melhorar": Fundição e Queima nunca liberam; a Torre nunca é construída; o Cofre não passa do 1. A aba Obras da Ferraria é só leitura. | O clique no lote abre **sempre** o cartão da construção (que já tem a ação grande: Fundir, Abrir o Cofrinho, Abrir a Torre, Tema de amanhã); atalho direto só pelo NPC (Ferreiro → Ferraria, Comerciante → Mercado) e pela hotbar. |
| A7 | `bank.ts:27-48`, `goalsService.ts:81,169`, `englishBase.ts` (efeitos do Cofre), `Cofrinho.tsx:163` (edição em andamento) | Juros de 5/8/12% por nível do Cofre (`vaultInterestRatePct`) no lugar de `settings/economy.interestRatePct`; `vaultGoalCap` 0 no Cofre nível 0 enquanto a tela oferece "Nova meta" por `maxOpenGoals` (a criança preenche e recebe erro); `modules.interest` ignorado; juros sobre o `savedGold` atual e não sobre o que estava guardado antes da semana. Decisão 3 da seção 1 (não reabrir): 5% por semana, teto 20. | Taxa única de `settings/economy`; Cofre nível 1 = 1 meta, 2 = 2 metas e bônus, 3 = faixa temporada e Extrato mensal, como `VILA_CONSTRUCOES`; a tela usa o mesmo limite do serviço; `applyWeeklyInterest` e `depositGoal` checam `modules.bank`/`interest`; base dos juros = guardado menos depósitos da semana corrente. |
| A8 | `Agenda.tsx`, `agenda.ts`, `VillageHome.tsx`, `Casa.tsx`, `HeroHeader` | A Agenda não alimenta nada do que a seção 13 promete: sem "Hoje você tem" e "Amanhã:" na Placa (`noticesForNow` não recebe agenda); chip do próximo evento nunca passado ao cabeçalho; alarme no app inexistente (`reminderDue` sem chamador: nada toca, nada pisca); sem Linha do dia na Casa nem `dayTimeline` com testes; sem Foco (`FlashTimer` órfão); formulário sem lembrete, repetir toda semana e nota (`remindMinutesBefore` nunca é gravado); aba Mês é uma lista de 62 dias, não o calendário (`CalendarModal` órfão); plano de estudo sem mostrar os 3 blocos nem "Não"; sem editar/apagar; `weeklyOrganizedBonus` não existe. | Fazer a seção 13 inteira: `dayTimeline` puro com testes; bloco "Hoje você tem" e "Amanhã" na Placa; `nextEvents` no cabeçalho; efeito de 60 s chamando `reminderDue` com som, toast "Ok", item piscando e fala do Olheiro; Foco dentro da Agenda reaproveitando `FlashTimer`; formulário completo; Mês absorvendo o `CalendarModal`; plano de estudo com lista dos dias e recusa; editar/apagar; bônus da semana organizada. |
| A9 | `shop.ts:35` (`canCraft`), `Oficina.tsx:106`, `villageService.craftGear` | Lanterna forjável (4 redstone + 2 ferro) e nenhum código lê `gear.lamp`: "em breve" que gasta material, contra `VILA_ITENS` ("equipamento sem efeito não pode ser forjado"). | `reason: 'soon'` para `lamp` até o efeito existir (contratos e prova de amanhã, dica grátis `hint:<date>`); botão desabilitado "Abre quando a Lanterna funcionar"; mesma trava no serviço. |
| A10 | `config/village.ts:106` (`DEFAULT_MODULES.bank = false`), `Cofrinho.tsx:40` | Com o Banco desligado por padrão, três atalhos viram beco: chip de gold → placa "O Cofre ainda está sendo cavado" (o Extrato não deveria depender do módulo), "Criar meta no Banco" nos prêmios → placa, "Comprar" sem gold na Loja → placa. Eu já liguei `settings/modules.bank = true` para o teste. | Gate do módulo só na aba Cofrinho e no atalho "Criar meta" (que some ou vira "Faltam N gold" com o Banco desligado); Extrato e Paciência sempre abertos; `bank: true` no padrão, salvo decisão contrária do pai. |

## 3. Severidade média

Dinheiro, regras e função:

- M1 `goalsService.ts:162`, `bank.ts:147` bônus pago sobre depósito da própria semana (`createGoal` não grava `lastInterestWeek`; base é o `savedGold` atual). Gravar `lastInterestWeek` na criação; calcular a base descontando os `goal_deposit` da semana.
- M2 `caps.ts:27` juros nunca consomem o teto do gold do jogo (linha `goal_interest` tem `amount` 0). Somar `metadata.savedAfter - savedBefore` para essa fonte; teste.
- M3 `goalsService.ts:266` meta alcançada sem `rewardId` cria `redemptions` com `rewardId = goalId`, invisível na tela de pedidos da criança e do pai (`getRewardById` devolve nulo). Gravar `rewardTitle`/ícone no pedido e ler com fallback.
- M4 `villageService.ts:757` Comerciante com o teto do dia batido: debita o material, gasta a venda do dia e paga 0. Recusar antes de debitar ("teto de hoje atingido; venda amanhã"); mesma proteção no Baú das tochas.
- M5 `dailyRulesService.ts:258` rachadura aparece em férias e folga. `missedIds = []` quando `vacation || paused` (manter na punição).
- M6 `dailyRulesService.ts:188` capacete absorve 1 de N perdidas (spec: só quando a perdida é a única) e a rachadura cai no período da missão perdoada. Condicionar a `missed === 1`; calcular o lote a partir de `missedIds` depois do capacete.
- M7 `functions/src/index.ts:126-162` modelo, `max_tokens` e tamanho do texto de TTS vêm do cliente sem limite: qualquer conta logada pode pedir modelo caro com 30 mil tokens. Lista fechada de modelos, `max_tokens` até 4000, TTS até 300 caracteres.
- M8 `functions/src/index.ts:178` `getSignedUrl` no TTS exige `iam.serviceAccounts.signBlob` na conta da função; tende a falhar. Trocar por token de download (`getDownloadURL` do admin) ou `makePublic`. Eu publiquei a função; o teste real está na seção 8.
- M9 `englishAi.ts:142`, `englishJudge.ts:119`, `firestore.rules` (`aiUsage`) contagem dupla de IA (cliente e função) e a regra de `aiUsage` ainda com `write` para qualquer logado. Remover `recordUsage` do cliente; `write: isAdmin()`.
- M10 `functions/src/index.ts:66` TTS entra no teto de 800 chamadas (o cliente descontava a voz). Descontar `byModel` de voz ou não contar `calls` no TTS.
- M11 `functions/src/index.ts:213` lembrete semanal dispara em qualquer dia e uma vez na vida (`remindedAt` no doc base). Expandir a ocorrência com o mesmo dia da semana e gravar `remindedFor: <data>`.
- M12 `firestore.rules:50-56` a criança pode aprovar a própria missão proposta (`status` livre no update) e criar task com `xp` sem teto. `resource.data.status != 'proposed'` no update; `xp <= 10` no create.
- M13 `firestore.rules` (`challenges` create) desafio proposto pela criança sem teto de prêmio e o painel aprova sem mostrar o prêmio. `goldReward <= 20 && xpReward <= 20 && target >= 5`; mostrar "+X gold, +Y XP, meta N" na proposta.
- M14 `DailyQuiz.tsx:151`, `DataContext.tsx:1012` prova linear paga por `adjustUserXP`/`adjustUserGold` (escrita absoluta do saldo do estado do React, fora de transação), contra a decisão 2. `payQuizRewards` em transação com chave `quiz:<date>`.
- M15 `firestoreService.ts:588` reversão de dia já fechado não feita (item 4 da seção 5) e não registrada como pendência. Implementar ou registrar como bloqueante do Lote 2.
- M16 `englishBaseService.ts:628`, `Oficina.tsx:18`, `BuildingCard.tsx:57`, `DailyQuiz.tsx:145`, `DailyChecklist.tsx:191`, `Mercado.tsx:49` `settings/economy` ignorado (custo x2 fixo, `quizRewards` sem settings, `lateWindow` sem economy, R7 fixo em 45). Ler do `VillageContext`/`getSettings`; R7 real com `referenceIncome` dos 7 dias.
- M17 `Balanca.tsx:41` R7 calculado sobre 800 transações de todo o período (saldo em D minúsculo, alerta nunca dispara); sem gasto por ralo; reajuste altera prêmios inativos; sem `balance.ts` puro. Filtrar 7 dias; agrupar negativos; só ativos; extrair módulo com teste.
- M18 `villageService.ts:700` Baú das tochas: só o maior patamar (perde o de 7 se não abrir a tempo), sem cosmético a cada 21, `metadata.capped` errado; e a tela só o mostra dentro do modal do Baú do Dia, sem o conteúdo (diamante não aparece). Iterar 7/14/21; hotspot na cena com `chest_streak`, conteúdo em `ItemSlot`, som.

Telas:

- M19 `Mercado.tsx:141-153` compra sem confirmação ("Com 60 gold você leva... Sobram 40"); "Comprar" sem gold abre o Banco em silêncio; `buyCosmetic` auto-equipa e marca "Novo" ao mesmo tempo; "Equipar agora?" é um toast sem Sim/Não que pula de tela.
- M20 `Mochila.tsx:84` aba Roupas não equipa nem tira (sem "nenhum" para chapéu, capa, pet); "Ver na loja" fecha a Mochila e perde o rascunho; editor ainda com quadrados de cor; `LevelUpModal` e prêmios sem `ItemSlot`; molduras PNG, `badge-novo` e `slot-empty` existem e não são usados.
- M21 `Oficina.tsx:63` preview da Ferraria ignora o item selecionado; "Fundir" e "Ver todas as obras" do cartão abrem a Ferraria na aba Forjar (sem `initialTab`); aba Obras não abre o cartão; frase quebrada "Abre na Precisa da Fornalha..."; Cerca diz que precisa do "Baú".
- M22 `VillageHome.tsx:234` faixa "Hoje" oferece "Concluir" numa missão de período ainda fechado (sempre falha de manhã). Escolher a próxima com `periodAllowedAt` verdadeiro; senão "Próxima às 12h".
- M23 `YesterdaySummary.tsx:217` conserto virou botão que falha até todas as missões; o estorno nunca chega para quem não volta ao resumo. Chamar `repairLot` em `completeTask` quando `done >= due` e houver rachadura.
- M24 `HeroPanel.tsx:219` em punição a Vila some inteira e Biblioteca e Mina não abrem (decisão 5). Vila com `punished`: Mercado, Baú e Loja travados com uma linha; prova e Mina abertas sem pagar.
- M25 `Extrato.tsx:38` só totais por semana, título "2026-W38", sem linhas (o histórico saiu dos prêmios e não chegou aqui), sem frase do Sábio, `monthHonest` com `new Date()`.
- M26 `DesafiosCard.tsx:74` **decisão do pai (15/09, 15h20): tirar o botão "Propor desafio" e o formulário da tela da criança** até isso ter valor de verdade (volta com a Torre nível 3 e um desenho próprio, Lote 2 ou depois). O serviço `createChallenge` com `status: 'proposed'` e a aprovação no painel podem ficar, sem entrada na tela. Além disso: prazo "até 20/09" em vez de "até sábado", sem barra de progresso, prêmio só em gold.
- M27 `VillageHome.tsx:182`, `DailyChest.tsx:294` chip do próximo evento nunca passado; Baú do Dia sem porta antes das 18h (modal só quando pronto), sem "faltam 2h10".
- M28 `VillageScene.tsx:579` arte que o relatório diz consumida e o código não usa: `cofre-2/3` (sempre `cofre-1`), `fx_rachadura` (3 linhas, sem `mc-shake`), `chest_streak`, troféus, marcos, `badge-novo`; `clockDriftWarning` não aparece nem no Saúde nem na Placa.

## 4. Severidade baixa

Textos crus (tipos da agenda em minúsculo sem acento, "semana ISO", "papai", "Turno mínimo/completo", "Campinho abre na Etapa 4", "Inventário: construa o Baú"); chip de tochas com o ícone da Lanterna e rótulo "dias completos"; estados de item errados (picareta não forjada como "Em breve", Esmeralda e Diamante em "Só se ganha"); "Vender 10" ativo com 3 madeira; hotbar sem teclas 1 a 5; `metadata.capped` errado em desafio e Baú; `weeklyStatementsFor` com expressão sem efeito e histórico cortado em 500 linhas; Cerca 2 apaga rachadura repetida; `FlashTimer` e `CalendarModal` órfãos; `HOTBAR_ICONS.Oficina` sobrando; `VILA_API.md` sem as exportações novas de `goldTx.ts` e `claims.ts`; boot "Abrindo a vila" (index.html, main.tsx, App.tsx, boot.css, bootOverlay, useDismissBoot, LoadingSpinner, ReadyBoot no painel, +195 linhas de miner.css) e `BaseMap.tsx` tocados fora do escopo sem registro no relatório.

## 5. Conferência de conexões (regra 7 do protocolo)

Existem no código: missões → desafios (`bumpChallenge`) e streak; prova → desafios e esmeralda 8/8; Mina → desafios e TTS pela função; fechamento → capacete, tochas, punição estendendo desafios, Cerca 1 e 3, rachaduras; conserto → estorno com chave; missão recuperada → linha `late_task`; Baú do Dia → Armazém 2/3, material mais escasso, teto; Baú das tochas → diamante e chave; Comerciante → `merchant_sale` com limite diário; Cofrinho → depósito, juros ao abrir e na virada do dia, cancelamento, fechamento pelo pai; tetos → chest, streak, challenge, interest, merchant, repair; chip gold → Extrato, chip nível → Torre, avatar → Mochila; prêmios e Loja → "Criar meta"; Mercado → prêmios embutidos e Comerciante; compra → "Novo" na Mochila; Mochila ↔ Ferraria; cena → Casa, Cofre, NPCs, Arena (fala do Olheiro); relógio → `dayChanged` ouvido por DataContext, HeroPanel e VillageHome; lint que proíbe hora fora do relógio.

Não existem (prometidas): Agenda → Placa, cabeçalho, Casa (Linha do dia), alarme no app, Foco, semana organizada; Lanterna → contratos e prova de amanhã; punição → Vila com rachaduras e prova/Mina abertas; conserto automático ao concluir; aba Obras → cartão; Baú das tochas na cena; `achievement` (resgate de conquista) fora de transação e fora do teto; ChatFlashGPT ainda lê a chave no cliente (desligado).

## 6. Fora do escopo e sem registro

Tela de abertura "Abrindo a vila" no app inteiro (inclusive `ReadyBoot` no painel), mudanças em `BaseMap.tsx`, `DailyChecklist` trocando `mc-panel` por `mc-inv`, e as edições de 14h50 a 15h00 (A4 a A7). O pai gostou de parte do visual; o que precisa é registrar no relatório e não misturar com o lote.

## 7. O que o Cursor faz agora, nesta ordem

0. Parar os ajustes visuais e **desfazer A4, A6 e A7**; A5 fica como está com os ajustes da tabela (decisão do pai).
1. A1, A2, A3, A11, A12 (dinheiro e forja), A9, A10.
2. A8 (a Agenda inteira da seção 13).
3. M1 a M18 e M29 a M38 (dinheiro, regras, função), depois M19 a M28 (telas), depois os baixos.
4. Rodar `npx tsc --noEmit -p tsconfig.app.json`, `npx eslint src --max-warnings 6`, `npm run test:english`, `npx vite build`, `node scripts/econ-sim.mjs`; republicar regras e função (`npx firebase-tools deploy --only firestore:rules,firestore:indexes,functions --project app-heitor`).
5. Fazer o aceite da seção 10 na conta de teste (Playwright já existe no scratchpad; eu já fiz um roteiro, seção 8) com fotos em `docs/exemplos/telas/etapa2/`.
6. Reescrever `RELATORIO_ETAPA_2_LOTE_1.md` com decisões, saídas dos comandos, fotos, o que foi tocado fora do escopo e a conferência de conexões. Só então o Lote 2.

## 8. Teste no navegador (conta de teste, 14h57 às 15h35)

Feito sobre a árvore em edição (a cena foi redesenhada duas vezes durante o teste e alguém construiu Fornalha e Armazém na conta de teste no meio); fotos e logs em `scratchpad/e2e2/` (fora do repositório). Conta resetada ao final.

**Funcionou**: relógio no cabeçalho e chips abrindo Extrato, Torre e Mochila; Casa abre pela casa da cena e pela hotbar, concluir missão paga certo (toast, chip, `task_completion`, material); Cofre constrói pelo cartão e abre o Banco; meta criada, depósito de 20 com `goal_deposit` tipo `saved`, depósito maior que o saldo recusado, limite de metas por nível respeitado, terceira meta recusada; juros idempotentes (segunda recarga não paga); painel: pedido de cancelamento aparece, "Cancelar e devolver" devolve exatamente o guardado (`goal_withdraw`), "Alcançada" cria o resgate aprovado e zera; desafio paga uma vez com chave em `claimed` e não paga de novo; Baú do Dia com 3/3 e `?h=19` abre, mostra Gold 10 e Madeira 2 (o mais escasso) em `ItemSlot`, segunda abertura recusada; Mochila com 4 abas por tecla I, avatar e prop; Ferraria com chips tenho/preciso, preview e Fundição presa à Fornalha 2; Comerciante limita a 2 vendas por dia; missão recuperada aparece só até meio-dia e paga metade sem material; conserto paga metade da penalidade e apaga a rachadura; Agenda cria compromisso, plano de estudo cria 3 missões com `date`, "Feito" paga 5 XP uma vez e nenhum gold; Mercado com prêmios embutidos, "Meus pedidos" e o atalho "Criar meta no Banco" preenchido; Loja em "Em breve"; sem `sk-` no bundle.

**Falhou (novo em relação às seções 2 e 3)**:

| # | Problema | Onde |
|---|---|---|
| A11 | **Forjar não funciona**: a tela usa a curva nova (nível 5 com 300 XP) e `craftGear` usa `getLegacyLevelFromXP` (curva antiga), então o botão "Forjar" habilita e o serviço responde "Nível 5" (com `pageerror`); nada é forjado. | `villageService.ts:369` |
| A12 | **Missão da Agenda paga gold**: `completeTask` usa `task.gold \|\| 5`, e a missão "Foco" tem `gold: 0` (0 é falso em JS): pagou 5 gold e o cartão mostra "+5 GOLD". Fere "organização nunca paga gold". | `DataContext.tsx` (`completeTask`) e o cartão da missão |
| M29 | Balança conta o depósito no Cofrinho como "Gastou" e como "Guardou" ao mesmo tempo ("Gastou 30 · Guardou 30"); "Poupança 333%" e "400%" no Extrato porque o gold semeado não é ganho. | `Balanca.tsx`, `bank.ts` (`weeklyStatement`) |
| M30 | Comerciante com o teto batido: primeira venda pagou 2 (cortada pelo teto), a segunda pagou 0, consumiu 10 madeira e a vaga, sem transação nem aviso (confirma M4). | `villageService.ts` (`sellMaterials`) |
| M31 | Chip de gold abre o Banco antes de o Cofre existir e a aba Cofrinho mostra "0% por semana" sem formulário e sem dizer que precisa construir o Cofre. | `Cofrinho.tsx` |
| M32 | `village.newItems` não recebe o conteúdo do Baú: "Novo" nunca aparece na Mochila. | `openDailyChest` |
| M33 | Lote Armazém abre o cartão da obra e não a Mochila (`VILA_MAPA`). | `VillageHome.tsx` |
| M34 | Aba Equipamentos da Mochila mostra "Em breve" para a picareta de pedra, que dá para forjar; capacete e lanterna "Bloqueado" por nível, não pela regra "até o efeito existir". | `Mochila.tsx`, `Oficina.tsx` |
| M35 | Rejeições não tratadas viram `pageerror`: `PERIOD_LOCKED` ao concluir fora do período (o `completeTask` relança depois do toast) e "Nível 5" na forja. | `DataContext.tsx`, `Oficina.tsx` |
| M36 | Segundo conserto no mesmo dia responde "Faça todas as missões de hoje para consertar" com tudo feito (a causa é "já consertado"). | `villageService.ts` (`repairLot`) |
| M37 | Prêmio "Faça 5 missões" numa conta com 3 missões por dia nunca libera: o gate usa `redeemMinTasks` fixo em vez de `min(redeemMinTasks, due)`. | `RewardsPanel`, `DataContext.redeemReward` |
| M38 | R7 inconsistente: Mercado diz "cerca de 45 gold por dia" (reserva) e o RewardForm "cerca de 1" (calculado). | `Mercado.tsx`, `RewardForm.tsx` |

Confirmados pelo teste (já nas seções 2 e 3): A1 (recuperação pagou de novo uma missão feita ontem e hoje), A1 (recuperação sobrescreveu `lastCompletedDate` e a missão foi paga de novo hoje: gold 101 para 106), A3 (as três missões Foco de 15, 16 e 17/09 aparecem todas hoje), A5 (Cofre, Agenda e Mercado como lotes a construir; hotbar com 4 botões), A8 (Mês é lista, sem Foco, sem "Hoje você tem", sem chip), M1 (juros na mesma semana do depósito), M4/M30, M26 (painel de desafios só com 5 modelos), A7 (8% com Cofre nível 2).

Usabilidade vista no teste: rótulos da cena se atropelam ("Mochila", "Heitor" e "Biblioteca" juntos; "Arena" sobre "Olheiro"; "Mina" atrás do Sábio); o cartão "Hoje" e os Desafios ficam abaixo da dobra, atrás do dock fixo; estados em inglês no painel ("open", "cancel_requested", "achieved"); tipos crus na Agenda; datas como "2026-09-18"; o modal do presente de nível trava o resto até escolher; toast "+1 pedra" quando a missão extra deu 2.

Não testado: prova e TTS pela função (publicada depois do teste; o TTS falha, ver seção 9), capacete, esmeralda na 3ª tocha, Baú das 7 tochas, punição, cerimônia de forja.

## 9. Feito por mim

- Google: liguei Secret Manager, Cloud Functions, Cloud Build, Artifact Registry, Cloud Scheduler, Cloud Run, Eventarc e Pub/Sub no projeto; criei o segredo `OPENAI_API_KEY`; publiquei `openai` e `agendaReminders` em `southamerica-east1` (Deploy complete). Sem isso a prova do dia não geraria na versão do dia 18.
- Firestore: `settings/modules.bank = true` (para o teste; o padrão do código é decisão do pai, A10).
- Teste da função publicada com o token da conta de teste: **chat 200** (devolveu o JSON pedido, o segredo e o teto funcionam), **sem login 401** (auth obrigatório funciona), **TTS 500 INTERNAL**: confirma o M8 (o `getSignedUrl` precisa de `iam.serviceAccounts.signBlob` que a conta da função não tem). Enquanto não corrigir, a voz da Mina cai no fallback. Correção pelo Cursor: token de download do admin (`getDownloadURL`) ou `makePublic()`, e registrar no relatório uma chamada real de TTS depois de republicar.
- Nenhuma mudança de código (o Cursor está com os arquivos abertos).


## 10. Segunda passada (15/09, 17h; branch `etapa-2`, commit 64532f6)

O Cursor executou a seção 7 e reescreveu o relatório. Checagens: tsc limpo, eslint só os 6 avisos antigos, 11 arquivos de teste (com casos novos para juros e teto), build ok, simulador ok, função republicada e TTS real com 200. Dois revisores conferiram item por item; o teste no navegador está na seção 11.

**Feito de verdade** (com arquivo e linha conferidos): A1 (missão recuperada confere a completion de ontem e não sobrescreve a data de hoje), A2 (fechamento, Baú e conserto contam só devidas), A6 (lote sempre abre o cartão; aba Obras abre o cartão), A9 (lanterna travada), A11 (forja e Loja na curva nova), A12 (gold 0 respeitado; missão da Agenda nunca paga gold), M1, M2, M4/M30, M5, M6, M7, M8, M9, M10, M11, M12, M14 (prova em transação com chave), M32, M33, M34, M36, M38, decisão sobre "Propor desafio" (M26), Baú das tochas na cena com patamares 7/14/21, Linha do dia na Casa, "Hoje você tem" e "Amanhã" na Placa, chip do próximo evento, formulário da Agenda com lembrete/repetir/nota, aba Mês como calendário, Foco 15/25 na Agenda, Loja com confirmação "Com X gold você leva... Sobram Y" e "Faltam N gold", Mochila equipa e tira, punição sem sumir a Vila, Extrato com movimentos e rótulos legíveis, teclas 1 a 5, textos crus corrigidos.

**Ainda alto (bloqueia)**:

| # | Onde | Problema | Correção |
|---|---|---|---|
| A4 (não feito) | `VillageHome.tsx:222-262, 395-404, 86-113`, `DailyQuiz.tsx:86-94`, `BuildingCard.tsx:322-328` | O portão da prova só é desenhado: `quizLocked` pinta o cadeado na cena, mas `openDistrict`, a hotbar e as teclas abrem Mina, Mercado, Ferraria e lotes sem a prova; com Mesa nível 0 não existe caminho para a prova (o único botão está no cartão da Mesa nível 1 ou mais), e a prova não abre sozinha quando obrigatória. O relatório diz "desfeito". | Em `openDistrict`, hotbar e teclas: se `quizLocked` e o destino for `mine`, `market`, `workshop`, `build:*`, `npc:ferreiro`, `npc:comerciante`, chamar `onOpenQuiz()`; "Prova do dia" em qualquer nível da Mesa; a Mesa nível 1 libera só o tema de amanhã. |
| R1 (regressão) | `firestoreService.ts:507,544,634`, `firestore.rules:214` | O id determinístico `uid_taskId_date` das `taskCompletions` faz a segunda conclusão de uma missão que o pai desfez no mesmo dia virar `update` de um doc existente, e a regra só deixa o admin atualizar: a transação inteira cai com "permission denied" e a criança vê "Sem internet: a missão não foi salva". Mesmo para o admin, o `set` apaga o registro revertido. | Manter o id determinístico só para conferir idempotência; quando o doc existir revertido, gravar um doc novo (sufixo ou id aleatório com o campo `key`); ou liberar na regra o update pela criança só quando `resource.data.reverted == true`. |
| R2 (regressão) | `VillageHome.tsx:142-166`, `agenda.ts:113-131` | O alarme da Agenda toca de novo a cada "Ok" e a cada minuto, e toca item já marcado como feito: nada no cliente grava `remindedFor`; `reminderDue` não olha `doneAt`; o efeito depende de `agendaFlash`, e limpar o flash re-dispara. Para item de ontem a função nem passa, então não para nunca com o app aberto. | Ao disparar, gravar `remindedFor: item.date` via `updateAgendaItem` (ou um conjunto de ids já tocados hoje em estado); `reminderDue` devolve falso com `doneAt`; um `AudioContext` só. |

**Parciais** (fechar junto): A3 (missões Foco continuam `active` para sempre depois da data: desativar ao passar a data), A5 (custo e hotbar feitos; conferir a Barraca com prêmios visíveis no cartão: hoje o "Criar meta no Banco" dentro do cartão do Mercado não faz nada e "Pedir" fica ativo num cartão que devia ser só leitura), A7 (Cofre nível 3 sem efeito: faixa temporada e Extrato mensal; `vaultInterestRatePct` órfã lendo o padrão; se o pai baixar `maxOpenGoals` a tela e o serviço discordam), A10 (o atalho "Criar meta no Banco" nos prêmios aparece com Cofre 0 e leva a uma placa perdendo o preset), M3 (`rewardTitle` gravado mas nenhuma tela lê: pedido de meta continua invisível), M13 (proposta sem prêmio no painel; baixo porque o botão saiu), M16 (`lateMissionUntilHour`, `quizRewards` e `nextCost` ainda com padrão fixo em pontos: cartão da obra lê `settings/economy` e `canBuild`/`buildUpgrade` leem o padrão, então com multiplicador 1 ou 3 no painel o cartão e o serviço discordam), M17 (Balança sem gasto por ralo), M18 (sem cosmético a cada 21; `newItems` recebe ids `trophy_*` que não existem no catálogo), M29 (taxa de poupança ainda estoura quando o depósito vem de gold semeado), M35 (`completeLateTask` sem try/catch: "Recuperar" não some depois do sucesso e o segundo clique vira `pageerror`), M37 (serviço usa `min(redeemMinTasks, due)`, a tela de prêmios ainda usa 5 fixo e nunca mostra "Pedir" com 3 devidas), Cerca 2 ainda apaga rachadura repetida, `metadata.capped` continua errado em desafio e Baú, chip de tochas ainda com o ícone da Lanterna e rótulo "dias completos", "Foco" na Linha do dia conclui a missão na hora sem cronômetro, Linha do dia mostra "Concluir" em missão feita e em período fechado, "Salvar" na Agenda com título vazio cria compromisso em branco que vai para a Placa e para o alarme, editar não consegue apagar hora ou nota, "Baú: abre às 18h (faltam Xh)" do rodapé calcula errado (às 15h50 diz 3h10), Mochila trancada na hotbar em punição mas não no hotspot, "Novo" some ao trocar de aba, `onFinished` do Foco dispara duas vezes em StrictMode, aba Obras oferece "Melhorar" para Barraca e Sino (nível máximo 1), push repetido uma vez para itens lembrados antes do deploy.

**Fora do escopo, registrado**: tela de abertura e `BaseMap` (o Cursor registrou). Não há restilização do painel além das abas.

**Conferência de conexões** do relatório: as linhas citadas existem (`completeTask` → desafios e streak e conserto automático; prova → `payQuizRewards` com chave; Mina → desafios e TTS; fechamento → capacete, tochas, punição, rachaduras; Baú do Dia e das tochas; Comerciante; Cofrinho e juros; tetos; Agenda → Placa, cabeçalho, alarme, Casa; relógio → `dayChanged`). O que a tabela afirma e não bate: "Prova: porta na cena" (só visual, A4).

**O que o Cursor faz agora**: A4, R1 e R2 primeiro; depois os parciais acima na ordem em que estão; rodar as checagens; republicar regras e função; refazer o aceite dos pontos tocados; atualizar o relatório. Depois disso o Lote 1 fecha e o Lote 2 pode começar.


## 11. Teste no navegador, segunda passada (15/09, 18h40 às 19h10; commit 64532f6)

Fotos e logs em `scratchpad/e2e3/`. Conta resetada ao final. Outra sessão usava a conta de teste ao mesmo tempo (meta "vcvfv", Cerca construída); não mudou nenhum veredito.

**Passou**: missão recuperada (só a perdida aparece, paga metade sem material uma vez, recusa a segunda, não mexe na data de hoje, some depois do meio-dia); extras e Foco (só a de hoje aparece, paga XP e material e zero gold, o Baú diz "Faltam 1 missões" e o conserto recusa com uma devida faltando); forja da picareta de pedra no nível 5 (materiais caem, `gear.pickaxe 1`, item no boneco) e lanterna travada nas duas pontas; cartão da construção (Fornalha 1 abre "Melhorar", Torre 0 "Construir", Ferreiro abre a Ferraria, Armazém abre a Mochila, Cofre 2 "Abrir o Cofrinho"); juros (nada na mesma semana do depósito; com semana antiga forçada paga 5% uma vez, com Cofre 2; segunda recarga não paga); Comerciante com o teto batido recusa sem consumir material e vende por 3 sem o teto; Agenda (lembrete, repetir, nota, plano com "Não", editar, apagar, "Hoje você tem" e "Amanhã" na Placa, chip no cabeçalho, Mês como calendário com passado e detalhe por dia, Foco 15 min rodando, Linha do dia com missões e compromissos por horário, "Amanhã você tem" no Fechar o dia, lembrete às 19h02 com toast, item piscando e fala do Olheiro); Mochila equipa e tira o boné e "nenhum"; Baú do Dia com porta antes das 18h ("faltam 4h00" no modal) e com 3/3 às 19h abre com o loot em `ItemSlot`, segunda abertura recusada; hotbar com Mercado e tecla 4, Loja em "Em breve", com a Barraca no nível 0 o cartão de construir mostra os prêmios; Extrato com movimentos e rótulos; Balança em 7 dias sem contar o depósito como gasto; `PERIOD_LOCKED` não vira mais `pageerror`.

**Falhou**:

| # | Problema | Onde |
|---|---|---|
| D1 (alta, dinheiro) | **Conserto automático paga 0**: `completeTask` chama `repairLot(uid, hoje)` e o serviço lê a penalidade em `dailyProgress` de **hoje** (0), grava a chave `repair:<hoje>`, zera as rachaduras e não cria linha nem toast. A criança perde o estorno de metade da penalidade de ontem e o botão manual não tem mais rachadura para consertar. | `DataContext.tsx:446`, `villageService.ts` (`repairLot`) |
| D1b (alta, regra) | **Prova nunca vira portão, causa raiz**: `subscribeToUserProgress` mapeia `quizEnabled` e **não mapeia `quizRequired`** (nem `quizQuestionCount`), então o cliente nunca vê o flag; com o portão forçado, `VillageScene` chama `onClickSpot` sem olhar `gated` e `openDistrict`, hotbar e teclas não consultam `quizLocked`; o cadeado da Mina não aparece. Confirma A4. | `firestoreService.ts:1552`, `VillageHome.tsx`, `VillageScene.tsx` |
| D2 (média, regra) | **Punição contornável**: teclas 4, L, B e I, o lote do Mercado ("Abrir o Mercado") e a hotbar Mochila abrem o que a linha diz estar fechado; só `market`, `npc:comerciante`, `chest` e `pack` do `openDistrict` bloqueiam. "Tarefas da punição" dispara "Missing or insufficient permissions" no listener de `punishmentTaskCompletions` (D6). | `VillageHome.tsx`, `firestore.rules` |
| D3 (média) | "Recuperar" continua na tela depois de recuperar e o segundo clique vira `pageerror: Missão já recuperada` sem toast (confirma o parcial M35). | `DailyChecklist.tsx`, `DataContext.completeLateTask` |
| D4 (média) | Lembrete da Agenda repete: dois toasts na hora e, 80 s depois do "Ok", toast e piscar de novo; o doc fica sem `remindedAt`/`remindedFor` (confirma R2). | `VillageHome.tsx:142-166` |
| D5 (média, tela) | "Novo" nunca aparece na Mochila: a troca de aba chama `seeItems()` antes do primeiro render e apaga tudo (picareta forjada e loot do Baú). | `Mochila.tsx:60` |
| D7 (média, regra) | Tela de prêmios: "Trocas bloqueadas 4/5 missões feitas hoje" numa conta com 3 devidas (somou a Foco e exige 5 fixo). Confirma o parcial M37. | `RewardsPanel.tsx` |
| D8 (baixa) | Rodapé do Baú: "faltam 4h60" (a conta do modal está certa; a do dock não). | `VillageHome.tsx:384` |
| D9 (baixa) | Lanterna sem rótulo "Em breve" na grade da Ferraria (só o botão avisa); Cofre 2 com "Abre na Precisa da Fornalha…"; toast "+1 pedra" quando a extra deu 2; "Semana 2026-38" no Extrato (ainda código); "Guardou 227%" quando o depósito vem de saldo antigo; fontes em inglês na Balança; formulário da Agenda mantém lembrete e repetir do item anterior. | vários |

Nota de desenvolvimento: o login navega para `/flash` e perde a query da URL, então `?quiz=lock` e `?onboard=1` nunca funcionam na prática (só `?h=` e `?d=`, lidos na montagem do relógio). Para testar o portão, usar `progress.quizRequired` de verdade (que hoje não é lido).

## 12. Veredito da segunda passada e o que falta

O Lote 1 está a uma rodada curta de fechar. Bloqueiam: **D1** (conserto pagando 0), **D1b/A4** (portão da prova: mapear `quizRequired`, portão em `openDistrict`, hotbar, teclas e no clique da cena, cadeado da Mina, "Prova do dia" em qualquer nível da Mesa), **R1** (id fixo das conclusões barrando a criança ao refazer missão desfeita; seção 10), **D2** (punição contornável) e **D4/R2** (alarme repetindo). Junto: D3, D5, D7, D8, D9 e os parciais da seção 10 (Foco ativa para sempre, "Criar meta" com Cofre 0, Cofre nível 3, `rewardTitle` lido nas telas, `settings/economy` no serviço de construção, gasto por ralo na Balança, `trophy_*` em `newItems`, Cerca 2, `metadata.capped`, chip de tochas, "Foco" na Casa sem cronômetro, "Concluir" em missão feita, título vazio na Agenda, editar apagando hora, Mochila na punição, `onFinished` duplo, Obras oferecendo "Melhorar" para Barraca e Sino).

Depois dessa rodada: checagens, republicar regras e função, aceite só dos pontos tocados (D1, D1b, D2, D4 com foto), relatório atualizado. Aí o Lote 1 fecha e o Lote 2 começa.
