# Revisão da Etapa 2, Lote 2

16/09/2026, manhã. Working tree sobre `be66428` (Lote 1 aprovado), branch `etapa-2`, sem commit. Relatório do Cursor: `RELATORIO_ETAPA_2_LOTE_2.md`. Especificação: `ETAPA_2_BANCO_E_TEMPORADA.md` (seções 6, 7, 13 a 15, 19, 20, 20b, 22) e os documentos de desenho (`VILA_CONSTRUCOES.md`, `VILA_ITENS.md`, `VILA_MAPA.md`, `VILA_CONQUISTAS.md`, `MUNDO.md`). Método: checagens mecânicas, três revisores independentes (dinheiro, temporada e conquistas; aderência com conferência de conexões e regra de cena; experiência da criança) e teste no navegador na conta de teste (seção 8, entra quando terminar).

## 1. Veredito

**Não aprovado.** O lote entregou muita coisa e a parte de dinheiro continua sã: nenhum caminho novo grava gold (conquistas, check-in, plano, amizade, pedidos, troféu e temporada pagam só XP, material, raro ou cosmético), tudo em transação com chave, check-in e plano com as regras certas, missão própria sem gold e sem aprovação pela criança. Mas três coisas impedem de fechar:

1. **Regressão do portão da prova**, aprovado no Lote 1 (seção 13 da revisão anterior): Mina, Ferreiro e Comerciante saíram da lista de destinos bloqueados, e com a Mesa "em ruínas" o portão inteiro some.
2. **Uma mecânica nova de "ruínas" entrou sem estar em lugar nenhum** (spec, desenho, roadmap, decisões do relatório): obra construída cai por missão perdida (uma por missão, inclusive em dia de punição), obra caída vale nível 0 e tranca Baú do Dia, Baú das tochas, Loja, Comerciante, Ferraria, criar meta, depositar, juros da semana, construir e tema de amanhã, e o reparo cobra material. Contraria três regras fechadas: "penalidade nunca alcança o Cofrinho", "aprender não é castigo" (a prova fecha com a Mesa caída) e "dia perdido deixa 1 lote rachado, só visual, com estorno ao consertar". O teste do Lote 1 foi reescrito em silêncio para caber nisso.
3. **Metade do catálogo de conquistas e quase todos os pedidos dos NPCs apontam para contadores que nada incrementa**: "Dia completo", "Tochas", "Meta batida", "Sete tochas", reflexões, palavras dominadas, o pedido 1 do Comerciante ("Traga 5 pedra", que não tem ação possível) e do Olheiro nunca acontecem. A Torre mostra "0/3" para sempre; os recordes ficam em zero porque `village.records` não é gravado; o Mapa de habilidades fica vazio porque o relatório lê as respostas da prova num formato que não existe.

Além disso o lote tocou o que a seção 11 proíbe, sem registro: `english/**` reescrito (a Mina ganhou uma aba Redstone com serviço próprio, 1.063 linhas, que é Etapa 3), a Arena virou construção com custo (é objeto fixo "Em breve"), o Campinho sumiu da cena (o lote virou `arena`), e o canvas ganhou `if` por id (`arena`, `torre`), contra a regra de cena da seção 20b e do `MUNDO.md`. Esses arquivos têm data posterior ao relatório.

## 2. Severidade alta (bloqueiam o Lote 2)

| # | Onde | Problema | Correção | Quem |
|---|---|---|---|---|
| A1 | `VillageHome.tsx:51` (`quizBlocksDest`), `:252` | Prova deixou de ser portão da Mina, do Ferreiro e do Comerciante; `quizGate = quizLocked && !mesaDown` libera tudo com a Mesa caída. | Devolver `mine`, `npc:ferreiro`, `npc:comerciante` à lista; nunca liberar o destino por causa da Mesa. | pequeno ajuste |
| A2 | `repair.ts:86`, `dailyRulesService.ts:257-265`, `villageService.ts:349,402,439,492,764,831,974`, `goalsService.ts:86,125,185`, `englishBaseService.ts:536,693,721`, `DailyQuiz.tsx:95,102`, `etapa2.test.ts` | Mecânica de ruínas sem spec (descrita na seção 1). | **Decisão do pai (seção 7).** Recomendação: voltar à regra aprovada (1 lote rachado por dia perdido, só visual, conserto ao fazer todas as missões com estorro de metade da penalidade, nunca em férias ou folga). Se o pai quiser manter algo de "ruína", registrar em `VILA_CONSTRUCOES.md` com limites: no máximo 1 obra por dia, nunca em punição, nunca Cofre nem Mesa, Cerca caída não perde a proteção, juros e Baú do Dia nunca dependem de obra de pé, reparo sem custo além do dia completo, simulador rodado de novo. | cursor |
| A3 | `data/achievements.ts:56`, `statsBump.ts`, `dailyRulesService` (`closeDay` não conta `fullDays`), `saveReflection`, `finishGoal`, `applyWeeklyInterest`, `completeChallenge`, `openStreakChest`, `burnWood` | Cerca de 50 conquistas apontam para contadores nunca gravados (`fullDaysCount`, `fullDaysBest`, `perfectWeeks`, `smelts`, `burns`, `contractsPerfect`, `wordsMastered`, `contractsLetter/Note/Forge/Merchant`, `quizStreak`, `reflections`, `goalsAchieved`, `interestWeeks`, `saverWeeks`, `bigGoals`, `agendaPlanned`, `organizedWeeks`, `streakChests`, `emeraldsEver`, `diamondsEver`, `talksSameDay`, `nightComplete`); `focusBlocks` conta missão-foco em vez de bloco de Foco. | Incrementar cada contador na transação do evento correspondente (lista acima) ou tirar do catálogo o que não tem evento ainda (marcando a etapa em que volta). Teste: para cada conquista do catálogo existe pelo menos um ponto do código que incrementa o `stat`. | cursor |
| A4 | `data/npcQuests.ts:13-38`, `villageService.ts:1126` | Pedidos dos NPCs travam no capítulo 1 (Comerciante "Traga 5 pedra": não existe entrega de pedra; Olheiro `fullDaysBest` nunca gravado; `contractsWeek`, `perfectWeeks`, `themesSet`, `weekQuestion`, `challengesDone`, `noPunishDays` idem); pedidos avançam sem nível de amizade e os tiers sobem sem o presente de 3 e 5. | Pedido 1 do Comerciante vira "Faça 3 contratos" ou ganha uma ação de entrega no Mercado (5 pedra descontadas em transação); contadores da A3; gate do capítulo pelo tier; presente de tier 3 e 5 (cosmético do NPC ou raro, nunca gold) na transação de subida. | cursor |
| A5 | `season.ts:30`, `villageService.ts:1053`, `Torre.tsx:116` | `village.records` nunca é gravado: Recordes sempre 0 e o ouro do troféu usa a sequência atual em vez das tochas da semana. | `recordsAfterWeek` chamado no fechamento da semana (domingo à noite ou no `claimTrophy`) gravando `weekGold`, `fullDays`, `quizBest`; troféu ouro com as tochas **da semana** (contar `dailyProgress` completos da semana ISO). | pequeno ajuste |
| A6 | `learningService.ts:33-72`, `Torre.tsx` (Mapa) | Mapa de habilidades e cartão Prova sempre vazios: `answers` é lido como objeto com categoria e acerto, mas `dailyQuizzes` grava `string[]`; `wordsMastered` sempre 0 (`englishSessions` não lido); merge acumula categorias de semanas passadas; `fullDays` é a sequência atual; só calcula pelo botão Recalcular. | Ler `dailyQuizzes.questions[i].category` cruzando com `answers[i]`; palavras de `englishSessions`; doc por semana (`learning/{uid}/weeks/{week}`, como a decisão 10 do Lote 1 já apontava); calcular ao abrir o app no domingo à noite e no painel. | cursor |
| A7 | `VillageHome.tsx:583,637`, `BuildingCard.tsx:372,568` | Ferraria só abre pela tecla O: "Fundir", "Ver todas as obras" e o atalho da Mochila chamam `npc:ferreiro`, que agora só abre o balão. No tablet não há caminho para Forjar, Fundição e Obras. | Trocar as chamadas para `openDistrict('workshop')`. | pequeno ajuste |
| A8 | `schedule.ts:45` (`dueTasksOn`), `dailyRulesService.ts:88`, `villageService.ts:460,895` | Missão própria ainda `proposed` conta como devida: "Faltam 1 missões", Baú recusa, conserto recusa e o fechamento cobra penalidade e zera tochas até o pai aprovar. | `status` em `ScheduleTask`; `dueTasksOn` ignora `proposed`; mapear `status` nos três leitores. | pequeno ajuste |
| A9 | `villageService.ts:694-696,1105` | `closeSeason` tira as conquistas de temporada de `achievementsUnlocked` mas deixa `claimed['ach:nivel_*']`: na temporada 2 elas nunca destravam de novo (a Torre mostra "5/5" sem carimbo para sempre). | Apagar `claimed['ach:<id>']` (e `newAchievements`) para os ids `resetOnSeason` no `closeSeason`, ou usar chave `ach:<season>:<id>` nessas. | pequeno ajuste |
| A10 | `DailyChecklist.tsx:75`, `TaskItem`, `DataContext.tsx:453` | Plano do turno não muda nada que a criança vê: a lista não segue `village.plan.order`, a missão-foco não tem selo, e o toast mostra metade do material que ela recebeu. | Ordenar por `plan.order` quando `plan.date === hoje`; selo "Foco · 2x material" no `TaskItem`; toast com a quantidade real. | cursor |
| A11 | `Torre.tsx:60` | Torre níveis 1, 2 e 3 não mudam nada (as seis abas abrem em qualquer nível, inclusive 0 pelo chip): material gasto sem efeito, contra `VILA_CONSTRUCOES` 4. | Nível < 2: Recordes e Troféus com cadeado "Torre nível 2"; < 3: Mapa com cadeado "Torre nível 3"; nível 0 pelo chip: só Conquistas e a linha "Construa a Torre para ver recordes e troféus". | pequeno ajuste |
| A12 | `english/base/*.tsx`, `RedstoneBench.tsx`, `redstoneService.ts`, `village/redstone.ts`, `config/englishBase.ts:187`, `anchors.json:13`, `VillageScene.tsx:846,854` | Fora do escopo sem registro: Mina reescrita com aba Redstone (Etapa 3), Arena como construção paga (é objeto fixo "Em breve"), Campinho fora da cena, `if` por id no canvas (contra a regra de cena). | Separar em branch própria para o pai decidir (a Oficina de Redstone é da Etapa 3 e precisa da ficha pedagógica e do desenho); voltar `campinho` ao `anchors.json`; Arena como `spots` fixo; tirar os `if` por id do canvas. | cursor |

## 3. Severidade média

- M1 `Torre.tsx:35` limpa "Novo" na montagem (mesmo bug da Mochila); `Torre.tsx:86` conquistas cruas (ids em vez de nomes, sem `ItemSlot`, sem moldura por camada, sem barra, sem data nem recompensa), sem cadeado por nível.
- M2 `Torre.tsx:113` "Da vida real" é o `AchievementsBadges` antigo inteiro, sem "Pedir para o pai confirmar"; painel sem a lista só leitura das do jogo; `kind: 'real'` não existe.
- M3 `DataContext.tsx:388` conquista destrava sem toast, som nem fala do NPC dono da categoria.
- M4 `VillageHome.tsx:319,1175` subir de amizade é mudo (sem conversa especial com "Continuar"), as falas repetem a cada clique, o bônus de domínio conta como "primeira conversa do dia" e apaga a fala de primeira vez; "não repetir 14 dias" usa os últimos 14 ids, não 14 dias.
- M5 `VillageHome.tsx:452` Placa sem "Troféu da semana" no sábado, sem "Desafio vence amanhã" e sem o pedido ativo do NPC; troféu escondido e perdido se não for reclamado.
- M6 `Casa.tsx:295` Fechar o dia não faz as 4 perguntas como perguntas (checkbox nativo), botão ativo com campo vazio; `VillageHome.tsx:668` Plano e Fechar o dia sem porta na cena e Fechar trava depois de abrir o Baú.
- M7 `VillageHome.tsx:254` "Por hoje é isso" aparece cedo (cotas sem os 3 contratos), não é faixa, e à noite só Mercado e Baú fecham (feito diferente sem registrar).
- M8 `DailyChecklist.tsx:229`, `TaskManager.tsx:124-135`, `TaskForm.tsx` missão própria sem estado "aguardando o pai" na tela da criança, toast de desenvolvedor ("Tarefa criada com sucesso!"), período implícito; aprovar cria missão diária obrigatória; o pai não consegue marcar uma missão como extra.
- M9 `LevelUpModal.tsx:22` presente de nível só quando o XP vem de missão: conquistas, check-in, pedidos, prova e contratos pulam o modal (o M3 do Lote 1 continua).
- M10 `statsBump.ts:176` contadores fora da transação de origem (dispara e esquece, erro engolido): conquista pode não vir mesmo com o evento gravado.
- M11 `villageService.ts:658` `closeSeason` sem guarda de duplicidade no serviço (só o botão); data prevista deriva.
- M12 `HojeCard.tsx:47` lista "Meta batida" para metas já fechadas e não lista as que o pai precisa fechar.
- M13 `firestore.rules:308` a criança escreve `village` inteiro (`rare`, `owned`, `stars`, `season`, `achievementsUnlocked`, `npcs`) e `progress.availableGold` pelo console. Já era assim, mas o Lote 2 pôs muito mais valor nesses campos: restringir por `diff().affectedKeys()` como em `goals`.
- M14 `villageService.ts:780` Baú das 21 tochas entrega o cosmético de marco `milestone_10` em vez de um exclusivo do Baú; `:1053` troféu ouro com `records.fullDays || fullDays`.
- M15 `BuildingCard.tsx:564` painel de prêmios no cartão do Mercado ignora a punição (sobra da seção 20 dada como conferida).
- M16 `Torre.tsx:44` `progress.level` gravado e desatualizado: Torre e diálogos usam um nível, o cabeçalho outro.
- M17 `learningService.ts:72`, `VillageManager` "Saúde" e relatório: `lastLearningWeek` só pelo botão.

## 4. Severidade baixa

Textos e acabamento listados pelos revisores (ids no lugar de nomes na Torre, "Turno gravado" sem missão-foco, toast "+1 madeira" quando deu 2, chip de próximo evento, `VILA_API.md` com a assinatura antiga de `cracksAfterClose`, `campinho` escondido das Obras).

## 5. Conferência de conexões

Existem e conferem: Plano do turno → `savePlan` (antes das 12h, uma vez) e `focus` na transação da missão; Fechar o dia → `submitCheckin` (+5 XP no mesmo tx, 3 palavras, uma vez) e `sageReplyFor` na Placa; missão própria → `tasks` proposed gold 0 e painel Aprovar; extra → 2x material fora de `dueTasksOn`; conquistas → `applyVillageStats` com chave `ach:` e sem gold; amizade → `talkToNpc`; troféu → `claimTrophy` sábado 18h; temporada → `closeSeason`; relatório → `learning/{uid}`; cartão Hoje → abas. Prometidas e não feitas: Plano → lista ordenada e selo de foco (A10); conquistas → toast/som/NPC (M3); amizade → conversa especial e presente de tier (A4, M4); Placa ← troféu, desafio vencendo, pedido ativo (M5); Torre ← níveis da Torre (A11) e `records` (A5); Mapa ← relatório com dados reais (A6); `english/**` ← nada devia entrar (A12).

## 6. Regra de cena (seção 20b)

Entrou como dado: `npcSpots`, `growth[]`, hotspot `chest_streak` por `type`, `props`. Contra a regra: `if` por id para `arena` e `torre` no canvas (A12), campinho removido do JSON. O relatório é honesto sobre `SceneCanvas` não extraído; aceito neste lote.

## 7. Decisões que dependem do pai

1. **Ruínas** (A2): tirar e voltar à rachadura aprovada (recomendado) ou manter com os limites da tabela. Minha recomendação é tirar: a rotina já tem penalidade, tocha zerada e rachadura; travar Baú, Cofrinho, juros e prova por uma missão perdida vira castigo em cascata, o oposto do "consequência sem castigo" que combinamos para a Fazenda e para tudo.
2. **Oficina de Redstone antecipada** (A12): a aba Redstone na Mina é trabalho da Etapa 3 e entrou sem desenho nem ficha pedagógica. Recomendo mover para uma branch própria e revisar quando a Etapa 3 começar, com o desenho feito antes.
3. **Arena como construção e Campinho fora** (A12): recomendo voltar (Arena é objeto fixo "Em breve" no `VILA_MAPA.md`; Campinho é a construção 6, com efeito na Etapa 4).

## 8. Teste no navegador (conta de teste, 16/09, 8h30 às 9h15)

Fotos e logs em `scratchpad/e2e5/` (117 fotos). Ressalvas: o Cursor editava a working tree ao mesmo tempo (Redstone) e outra sessão usava a conta de teste; o reset da conta não zera os campos novos (`stats`, `npcs`, `achievementsUnlocked`, `plan`, `stars`, `trophies`, `records`, `learning`), então o agente zerou por REST. O reset precisa ganhar esses campos (pendência para o Cursor).

**Passou**: Plano do turno (fecha ao meio-dia, uma vez por dia, reordena, missão-foco paga material em dobro com `focus: true`); missão própria (proposta com gold 0 e XP 5, "Aprovar" no painel via cartão Hoje, concluir paga XP e material e zero gold); extra (2x material, não conta como devida, Baú diz "Faltam 1 missões" ignorando a extra); primeira conquista (toast uma vez, chave, sem gold, não repete); Torre com categorias e "Quase lá"; amizade (+1 só na primeira fala do dia, três falas diferentes, balão com nome); Fechar o dia (recusa 2 palavras, grava com 3, +5 XP, trava a segunda vez, o Sábio responde na Placa no dia seguinte); "Por hoje é isso" com tudo feito e Mercado e Baú trancados às 22h; troféu ouro no sábado com esmeralda e chave; fechar temporada (estrela, XP zero, gold intacto, conquistas de nível limpas, snapshot); vida na cena (Sábio na mina de dia e na fogueira à noite, Comerciante some às 21h com placa, minerador vira para o lote sob o mouse, reação ao concluir missão); painel (cartão Hoje com as pendências certas, quatro grupos, Recalcular grava `learning` e a Torre mostra o Mapa, Personagens, Saúde). Nenhum `pageerror`.

**Falhou ou ficou estranho**, confirmando a leitura do código: fechar temporada duas vezes duplica estrela e pula temporada (M11 vira **alta**); conquistas de nível não voltam na temporada seguinte (A9); missão proposta conta como devida (A8); metade dos contadores nunca incrementa: falou com os quatro NPCs no mesmo dia e "Curioso" não veio (A3); o plano não muda a lista nem marca a foco, e o toast diz "+1 madeira" quando pagou 2 (A10); "Novo" na Torre nunca aparece, escondidas não viram "?", a Torre usa `progress.level` parado em 1 ("Aprendiz 1/5" com o cabeçalho em nível 3) (M1, M16); "Da vida real" sem "Pedir para o pai confirmar" (M2); conversa especial de amizade não existe e a quarta fala do dia já repete com o banco vazio (M4); pedido ativo e troféu não aparecem na Placa (M5); item `milestone_10` duplicado no catálogo gera aviso do React no Mercado; erro 400 de commit durante a obra por duas transações em paralelo (M10); conquistas fora de missão destravam sem toast (M3); placa "volta às 7" num retângulo branco cortado; corações do balão são quadradinhos cinza; "Faltam 1 missões"; toast "Tarefa criada com sucesso!"; a cerimônia de obra e o pulo do NPC são quase invisíveis; camadas de crescimento não existem (arte minha, pendente).

Usabilidade: no Fechar o dia as quatro linhas são frases com caixinha, não perguntas ("bebi água?"), e o botão "Fechar" parece fechar a janela; depois de gravar o plano não dá para ver qual missão é a foco; conquistas em fileiras de texto sem ícone, moldura, data ou cor; "Da vida real" com slot cinza sem nome; categorias em minúsculo.


## 9. O que o Cursor faz agora, nesta ordem

0. Registrar as decisões do pai (seção 7) e desfazer o que ele mandar desfazer; devolver `campinho` ao `anchors.json`; tirar os `if` por id do canvas.
1. A1, A7, A8, A9, A5, A11, A13 (trava no closeSeason) e A14 (reset da conta de teste com os campos novos), na ordem.
2. A3 e A4 (contadores e pedidos: cada `stat` do catálogo com um ponto de incremento dentro da transação do evento; teste que varre o catálogo).
3. A6 (relatório e mapa com dados reais, doc por semana), A10 (plano visível na lista).
4. Médias M1 a M17, depois as baixas.
5. Checagens, republicar regras (M13), aceite na conta de teste com fotos do que mudou, relatório atualizado com decisões e conferência de conexões. Sem commit fora da `etapa-2`.

## 10. Veredito com o navegador

Confirma a seção 1: **não aprovado**. O que o navegador acrescentou: fechar temporada sem trava vira item alto (A13: guarda por `season` na transação e botão desabilitado), reset da conta de teste sem os campos novos (A14: `reset-test-account.cjs` zera `stats`, `npcs`, `achievementsUnlocked`, `newAchievements`, `plan`, `stars`, `trophies`, `records`, `checkin` e apaga `learning/{uid}`), e o `milestone_10` duplicado (baixo). A ordem da seção 9 continua; A13 e A14 entram no passo 1.
